// Generated by packages/daemon/scripts/build-mobile-bundle.mjs. Do not edit.
export const TERMUX_DAEMON_SOURCE = "// packages/daemon/src/index.ts\nimport { WebSocketServer } from \"ws\";\nimport { homedir as homedir2 } from \"node:os\";\nimport { join as join3 } from \"node:path\";\n\n// packages/shared/src/bridge-protocol.ts\nvar BRIDGE_SUBSCRIBE_PREFIX = \"subscribe-\";\nvar BRIDGE_CALLBACK_PREFIX = \"subscribe.callback-\";\nfunction createBridgeCallbackName(key, id) {\n  return `${BRIDGE_CALLBACK_PREFIX}${key}${id}`;\n}\nfunction parseBridgeRequestMessage(message) {\n  if (!message.name.startsWith(BRIDGE_SUBSCRIBE_PREFIX)) {\n    return null;\n  }\n  if (!isRecord(message.data) || typeof message.data.id !== \"string\") {\n    return null;\n  }\n  return {\n    key: message.name.slice(BRIDGE_SUBSCRIBE_PREFIX.length),\n    id: message.data.id,\n    data: message.data.data\n  };\n}\nfunction createBridgeCallbackMessage(key, id, data) {\n  return {\n    name: createBridgeCallbackName(key, id),\n    data\n  };\n}\nfunction isRecord(value) {\n  return typeof value === \"object\" && value !== null;\n}\n\n// packages/shared/src/types.ts\nvar AGENT_MODES = {\n  claude: [\n    { value: \"default\", label: \"Default\" },\n    { value: \"plan\", label: \"Plan\" },\n    { value: \"bypassPermissions\", label: \"YOLO\" }\n  ],\n  qwen: [\n    { value: \"default\", label: \"Default\" },\n    { value: \"yolo\", label: \"YOLO\" }\n  ],\n  opencode: [\n    { value: \"build\", label: \"Build\" },\n    { value: \"plan\", label: \"Plan\" }\n  ],\n  gemini: [\n    { value: \"default\", label: \"Default\" },\n    { value: \"autoEdit\", label: \"Auto-Accept Edits\" },\n    { value: \"yolo\", label: \"YOLO\" }\n  ],\n  codex: [\n    { value: \"default\", label: \"Plan\" },\n    { value: \"autoEdit\", label: \"Auto Edit\" },\n    { value: \"yolo\", label: \"Full Auto\" }\n  ],\n  cursor: [\n    { value: \"agent\", label: \"Agent\", description: \"Full agent capabilities with tool access\" },\n    { value: \"plan\", label: \"Plan\", description: \"Read-only mode for planning and designing before implementation\" },\n    { value: \"ask\", label: \"Ask\", description: \"Q&A mode - no edits or command execution\" }\n  ],\n  snow: [\n    { value: \"default\", label: \"Agent\" },\n    { value: \"yolo\", label: \"YOLO\" }\n  ]\n};\nfunction getAgentModes(backend) {\n  if (!backend) return [];\n  return [...AGENT_MODES[backend] || []];\n}\n\n// packages/daemon/src/default-router.ts\nimport { readFile as readFile2 } from \"node:fs/promises\";\nimport { homedir } from \"node:os\";\nimport { join as join2 } from \"node:path\";\n\n// packages/daemon/src/agent-adapters/codex-adapter.ts\nimport { relative } from \"node:path\";\nvar DEFAULT_CODEX_MODELS = [\n  { id: \"gpt-5-codex\", label: \"GPT-5 Codex\" },\n  { id: \"gpt-5\", label: \"GPT-5\" },\n  { id: \"gpt-5-mini\", label: \"GPT-5 Mini\" }\n];\nfunction buildCodexCommand(options) {\n  const args = [\"exec\", \"--json\", \"--skip-git-repo-check\"];\n  if (options.model) args.push(\"--model\", options.model);\n  if (options.approvalMode === \"yolo\") {\n    args.push(\"--dangerously-bypass-approvals-and-sandbox\");\n  } else {\n    args.push(\"--sandbox\", options.approvalMode === \"autoEdit\" ? \"workspace-write\" : \"read-only\");\n  }\n  args.push(options.prompt);\n  return { command: \"codex\", args };\n}\nfunction createCodexAdapter(runner) {\n  return {\n    backend: \"codex\",\n    name: \"codex\",\n    label: \"Codex CLI\",\n    async probe() {\n      const exists = await runner.commandExists(\"codex\");\n      if (!exists) return { backend: \"codex\", state: \"missing\", detail: \"codex command not found\" };\n      return {\n        backend: \"codex\",\n        state: \"ready\",\n        version: await runner.readVersion?.(\"codex\", [\"--version\"])\n      };\n    },\n    async getModelInfo() {\n      return {\n        currentModelId: null,\n        currentModelLabel: \"Default Codex model\",\n        availableModels: DEFAULT_CODEX_MODELS,\n        canSwitch: DEFAULT_CODEX_MODELS.length > 0,\n        source: \"models\"\n      };\n    },\n    async *sendMessage(input) {\n      const prompt = appendSelectedFilesToPrompt(input.input, input.files ?? [], input.workspace);\n      const command = buildCodexCommand({\n        prompt,\n        model: input.model,\n        approvalMode: input.sessionMode\n      });\n      yield {\n        type: \"thought\",\n        subject: \"Codex CLI\",\n        description: [command.command, ...command.args].join(\" \")\n      };\n      if (!runner.runCommand) {\n        yield {\n          type: \"content\",\n          content: \"Codex CLI execution is queued for the Termux runtime adapter.\"\n        };\n        return;\n      }\n      let lineBuffer = \"\";\n      let emittedContent = \"\";\n      const queue = createAsyncQueue();\n      const emitContent = (content) => {\n        if (!content) return;\n        emittedContent += content;\n        queue.push({ type: \"content\", content });\n      };\n      const handleEvent = (event) => {\n        emitContent(extractCodexEventText(event));\n        const tool = extractCodexToolUpdate(event);\n        if (tool) queue.push({ type: \"codex_tool_call\", data: tool });\n        const plan = extractCodexPlanUpdate(event);\n        if (plan) queue.push({ type: \"plan\", data: plan });\n      };\n      const handleStdout = (chunk) => {\n        const lines = (lineBuffer + chunk.toString()).split(/\\r?\\n/);\n        lineBuffer = lines.pop() ?? \"\";\n        for (const line of lines) {\n          handleEvent(parseCodexJsonEvent(line));\n        }\n      };\n      const run = runner.runCommand(command, {\n        cwd: input.workspace || process.env.HOME || process.cwd(),\n        signal: input.signal,\n        onStdout: handleStdout\n      });\n      const finalized = run.then(\n        (result) => {\n          handleEvent(parseCodexJsonEvent(lineBuffer));\n          if (!emittedContent) {\n            const fallback = extractCodexJsonText(result.stdout) || result.stdout.trim();\n            if (fallback) emitContent(fallback);\n          }\n          queue.close();\n          return result;\n        },\n        (error) => {\n          queue.fail(error);\n          return { stdout: \"\", stderr: \"\" };\n        }\n      );\n      for await (const event of queue) {\n        yield event;\n      }\n      await finalized;\n    }\n  };\n}\nfunction parseCodexJsonEvent(line) {\n  if (!line.trim()) return null;\n  try {\n    return JSON.parse(line);\n  } catch {\n    return null;\n  }\n}\nfunction extractCodexEventText(value) {\n  if (typeof value === \"string\") return value;\n  if (!isRecord2(value)) return \"\";\n  const item = isRecord2(value.item) ? value.item : {};\n  if (value.type === \"item.completed\" && item.type === \"agent_message\" && typeof item.text === \"string\") {\n    return item.text;\n  }\n  if (value.type === \"error\" && typeof value.message === \"string\") return value.message;\n  if (value.type === \"turn.failed\" && isRecord2(value.error) && typeof value.error.message === \"string\") {\n    return value.error.message;\n  }\n  const direct = stringValue(value.text) || stringValue(value.content) || stringValue(value.delta) || (isRecord2(value.message) ? stringValue(value.message.content) : void 0);\n  if (direct) return direct;\n  if (isRecord2(value.data)) return extractCodexEventText(value.data);\n  return \"\";\n}\nfunction extractCodexToolUpdate(value) {\n  if (!isRecord2(value)) return null;\n  const type = typeof value.type === \"string\" ? value.type : \"\";\n  if (!(type === \"item.started\" || type === \"item.completed\")) return null;\n  const item = isRecord2(value.item) ? value.item : {};\n  if (item.type === \"agent_message\") return null;\n  if (item.type === \"todo_list\") return null;\n  if (item.type === \"command_execution\") return codexCommandExecutionTool(type, item);\n  if (item.type === \"web_search\") return codexWebSearchTool(type, item);\n  if (item.type === \"file_change\") return codexFileChangeTool(type, item);\n  if (item.type === \"mcp_tool_call\") return codexMcpToolCallTool(type, item);\n  return codexGenericTool(type, item);\n}\nfunction extractCodexPlanUpdate(value) {\n  if (!isRecord2(value)) return null;\n  const type = typeof value.type === \"string\" ? value.type : \"\";\n  if (!(type === \"item.started\" || type === \"item.updated\" || type === \"item.completed\")) return null;\n  const item = isRecord2(value.item) ? value.item : {};\n  if (item.type !== \"todo_list\") return null;\n  return codexTodoListPlan(item);\n}\nfunction extractCodexJsonText(stdout) {\n  return stdout.split(/\\r?\\n/).map((line) => extractCodexEventText(parseCodexJsonEvent(line))).join(\"\");\n}\nfunction codexTodoListPlan(item) {\n  const sessionId = stringValue(item.id);\n  const items = Array.isArray(item.items) ? item.items.filter(isRecord2) : [];\n  if (!sessionId) return null;\n  return {\n    sessionId,\n    entries: items.map((todo) => ({\n      title: stringValue(todo.text) || \"\",\n      status: todo.completed ? \"completed\" : \"pending\"\n    }))\n  };\n}\nfunction codexCommandExecutionTool(type, item) {\n  const toolCallId = stringValue(item.id);\n  if (!toolCallId) return null;\n  const command = stringValue(item.command) || \"command\";\n  const output = stringValue(item.aggregated_output) || \"\";\n  const description = [command, output.trim()].filter(Boolean).join(\"\\n\\n\");\n  return {\n    toolCallId,\n    kind: \"command_execution\",\n    title: command,\n    description,\n    status: codexToolStatus(type, item)\n  };\n}\nfunction codexWebSearchTool(type, item) {\n  const toolCallId = stringValue(item.id);\n  if (!toolCallId) return null;\n  const query = stringValue(item.query) || \"\";\n  return {\n    toolCallId,\n    kind: \"web_search\",\n    subtype: type === \"item.started\" ? \"web_search_begin\" : \"web_search_end\",\n    title: query || \"Web search\",\n    description: query,\n    status: type === \"item.started\" ? \"executing\" : \"success\",\n    data: { query }\n  };\n}\nfunction codexFileChangeTool(type, item) {\n  const toolCallId = stringValue(item.id);\n  if (!toolCallId) return null;\n  const changes = Array.isArray(item.changes) ? item.changes.filter(isRecord2) : [];\n  return {\n    toolCallId,\n    kind: \"file_change\",\n    title: codexFileChangeTitle(changes),\n    description: codexFileChangeDescription(item),\n    status: codexToolStatus(type, item),\n    data: { changes }\n  };\n}\nfunction codexMcpToolCallTool(type, item) {\n  const toolCallId = stringValue(item.id);\n  if (!toolCallId) return null;\n  const server2 = stringValue(item.server) || \"\";\n  const tool = stringValue(item.tool) || \"\";\n  return {\n    toolCallId,\n    kind: \"mcp_tool_call\",\n    title: [server2, tool].filter(Boolean).join(\"/\") || \"MCP tool call\",\n    description: codexMcpToolDescription(item),\n    status: codexToolStatus(type, item),\n    data: { server: server2, tool, arguments: item.arguments, result: item.result, error: item.error }\n  };\n}\nfunction codexGenericTool(type, item) {\n  const toolCallId = stringValue(item.id);\n  if (!toolCallId) return null;\n  const kind = stringValue(item.type) || \"tool\";\n  return {\n    toolCallId,\n    kind,\n    title: codexToolTitle(item, kind),\n    description: codexToolDescription(item),\n    status: codexToolStatus(type, item)\n  };\n}\nfunction codexFileChangeTitle(changes) {\n  if (changes.length === 1) return codexFileChangeLabel(changes[0]);\n  return changes.length ? `${changes.length} file changes` : \"File changes\";\n}\nfunction codexFileChangeDescription(item) {\n  const changes = Array.isArray(item.changes) ? item.changes.filter(isRecord2) : [];\n  return changes.map(codexFileChangeLabel).filter(Boolean).join(\"\\n\");\n}\nfunction codexFileChangeLabel(change) {\n  const path = stringValue(change.path);\n  const kind = stringValue(change.kind);\n  return [kind, path].filter(Boolean).join(\" \");\n}\nfunction codexMcpToolDescription(item) {\n  const parts = [];\n  if (item.arguments !== void 0) parts.push(`Arguments: ${safeJsonStringify(item.arguments)}`);\n  if (item.result !== void 0) parts.push(`Result: ${safeJsonStringify(item.result)}`);\n  if (isRecord2(item.error) && typeof item.error.message === \"string\") {\n    parts.push(`Error: ${item.error.message}`);\n  } else if (typeof item.error === \"string\") {\n    parts.push(`Error: ${item.error}`);\n  }\n  return parts.join(\"\\n\\n\");\n}\nfunction codexToolStatus(type, item) {\n  if (type === \"item.started\" || item.status === \"in_progress\") return \"executing\";\n  if (item.status === \"failed\" || item.status === \"error\") return \"error\";\n  if (typeof item.exit_code === \"number\" && item.exit_code !== 0) return \"error\";\n  if (type === \"item.completed\" || item.status === \"completed\") return \"success\";\n  if (item.status === \"canceled\" || item.status === \"cancelled\") return \"canceled\";\n  return \"pending\";\n}\nfunction codexToolTitle(item, fallback) {\n  return stringValue(item.title) || stringValue(item.name) || stringValue(item.command) || fallback;\n}\nfunction codexToolDescription(item) {\n  return stringValue(item.description) || stringValue(item.aggregated_output) || stringValue(item.output) || stringValue(item.error) || \"\";\n}\nfunction appendSelectedFilesToPrompt(input, files, workspace) {\n  if (files.length === 0) return input;\n  const root = workspace || process.cwd();\n  const fileList = files.map((filePath) => `- ${normalizeRelativePath(relative(root, filePath))}`).join(\"\\n\");\n  return `${input}\n\nSelected files:\n${fileList}`;\n}\nfunction normalizeRelativePath(path) {\n  return path.replace(/\\\\/g, \"/\");\n}\nfunction safeJsonStringify(value) {\n  try {\n    return JSON.stringify(value);\n  } catch {\n    return String(value);\n  }\n}\nfunction createAsyncQueue() {\n  const values = [];\n  const waiters = [];\n  let closed = false;\n  let failure;\n  const next = () => {\n    if (values.length > 0) return Promise.resolve({ value: values.shift(), done: false });\n    if (failure !== void 0) return Promise.reject(failure);\n    if (closed) return Promise.resolve({ value: void 0, done: true });\n    return new Promise((resolve2, reject) => {\n      waiters.push({ resolve: resolve2, reject });\n    });\n  };\n  return {\n    push(value) {\n      if (closed || failure !== void 0) return;\n      const waiter = waiters.shift();\n      if (waiter) {\n        waiter.resolve({ value, done: false });\n        return;\n      }\n      values.push(value);\n    },\n    close() {\n      if (closed) return;\n      closed = true;\n      for (const waiter of waiters.splice(0)) {\n        waiter.resolve({ value: void 0, done: true });\n      }\n    },\n    fail(error) {\n      if (closed || failure !== void 0) return;\n      failure = error;\n      for (const waiter of waiters.splice(0)) {\n        waiter.reject(error);\n      }\n    },\n    [Symbol.asyncIterator]() {\n      return { next };\n    }\n  };\n}\nfunction isRecord2(value) {\n  return typeof value === \"object\" && value !== null && !Array.isArray(value);\n}\nfunction stringValue(value) {\n  return typeof value === \"string\" && value.length > 0 ? value : void 0;\n}\n\n// packages/daemon/src/agent-adapters/gemini-adapter.ts\nimport { relative as relative2 } from \"node:path\";\nvar DEFAULT_GEMINI_MODELS = [\n  { id: \"gemini-2.5-pro\", label: \"Gemini 2.5 Pro\" },\n  { id: \"gemini-2.5-flash\", label: \"Gemini 2.5 Flash\" },\n  { id: \"gemini-2.0-flash\", label: \"Gemini 2.0 Flash\" }\n];\nfunction buildGeminiCommand(options) {\n  return {\n    command: \"gemini\",\n    args: [\n      \"-p\",\n      options.prompt,\n      \"--output-format\",\n      \"stream-json\",\n      ...options.model ? [\"--model\", options.model] : [],\n      ...options.approvalMode ? [\"--approval-mode\", options.approvalMode] : []\n    ]\n  };\n}\nfunction parseGeminiStreamJsonLine(line) {\n  let value;\n  try {\n    value = JSON.parse(line);\n  } catch {\n    return null;\n  }\n  const content = extractText(value);\n  return content ? { type: \"content\", content } : null;\n}\nfunction createGeminiAdapter(runner) {\n  return {\n    backend: \"gemini\",\n    name: \"gemini\",\n    label: \"Gemini CLI\",\n    async probe() {\n      const exists = await runner.commandExists(\"gemini\");\n      if (!exists) return { backend: \"gemini\", state: \"missing\", detail: \"gemini command not found\" };\n      return {\n        backend: \"gemini\",\n        state: \"ready\",\n        version: await runner.readVersion?.(\"gemini\", [\"--version\"])\n      };\n    },\n    async getModelInfo() {\n      return {\n        currentModelId: null,\n        currentModelLabel: \"Default Gemini model\",\n        availableModels: DEFAULT_GEMINI_MODELS,\n        canSwitch: DEFAULT_GEMINI_MODELS.length > 0,\n        source: \"models\"\n      };\n    },\n    async *sendMessage(input) {\n      const prompt = appendSelectedFilesToPrompt2(input.input, input.files ?? [], input.workspace);\n      const command = buildGeminiCommand({\n        prompt,\n        model: input.model,\n        approvalMode: input.sessionMode\n      });\n      yield {\n        type: \"thought\",\n        subject: \"Gemini CLI\",\n        description: [command.command, ...command.args].join(\" \")\n      };\n      if (!runner.runCommand) {\n        yield {\n          type: \"content\",\n          content: \"Gemini CLI execution is queued for the Termux runtime adapter.\"\n        };\n        return;\n      }\n      let lineBuffer = \"\";\n      let emittedContent = \"\";\n      const queue = createAsyncQueue2();\n      const emitContent = (content) => {\n        if (!content) return;\n        emittedContent += content;\n        queue.push({ type: \"content\", content });\n      };\n      const handleStdout = (chunk) => {\n        const lines = (lineBuffer + chunk.toString()).split(/\\r?\\n/);\n        lineBuffer = lines.pop() ?? \"\";\n        for (const line of lines) {\n          const parsed = parseGeminiStreamJsonLine(line);\n          if (parsed) emitContent(parsed.content);\n        }\n      };\n      const run = runner.runCommand(command, {\n        cwd: input.workspace || process.env.HOME || process.cwd(),\n        signal: input.signal,\n        onStdout: handleStdout\n      });\n      const finalized = run.then(\n        (result) => {\n          const tail = parseGeminiStreamJsonLine(lineBuffer);\n          if (tail) emitContent(tail.content);\n          if (!emittedContent) {\n            const fallback = extractGeminiStreamText(result.stdout) || result.stdout.trim();\n            if (fallback) emitContent(fallback);\n          }\n          queue.close();\n          return result;\n        },\n        (error) => {\n          queue.fail(error);\n          return { stdout: \"\", stderr: \"\" };\n        }\n      );\n      for await (const event of queue) {\n        yield event;\n      }\n      await finalized;\n    }\n  };\n}\nfunction extractGeminiStreamText(stdout) {\n  return stdout.split(/\\r?\\n/).map((line) => parseGeminiStreamJsonLine(line)?.content ?? \"\").join(\"\");\n}\nfunction appendSelectedFilesToPrompt2(input, files, workspace) {\n  if (files.length === 0) return input;\n  const root = workspace || process.cwd();\n  const fileList = files.map((filePath) => `- ${normalizeRelativePath2(relative2(root, filePath))}`).join(\"\\n\");\n  return `${input}\n\nSelected files:\n${fileList}`;\n}\nfunction normalizeRelativePath2(path) {\n  return path.replace(/\\\\/g, \"/\");\n}\nfunction createAsyncQueue2() {\n  const values = [];\n  const waiters = [];\n  let closed = false;\n  let failure;\n  const next = () => {\n    if (values.length > 0) return Promise.resolve({ value: values.shift(), done: false });\n    if (failure !== void 0) return Promise.reject(failure);\n    if (closed) return Promise.resolve({ value: void 0, done: true });\n    return new Promise((resolve2, reject) => {\n      waiters.push({ resolve: resolve2, reject });\n    });\n  };\n  return {\n    push(value) {\n      if (closed || failure !== void 0) return;\n      const waiter = waiters.shift();\n      if (waiter) {\n        waiter.resolve({ value, done: false });\n        return;\n      }\n      values.push(value);\n    },\n    close() {\n      if (closed) return;\n      closed = true;\n      for (const waiter of waiters.splice(0)) {\n        waiter.resolve({ value: void 0, done: true });\n      }\n    },\n    fail(error) {\n      if (closed || failure !== void 0) return;\n      failure = error;\n      for (const waiter of waiters.splice(0)) {\n        waiter.reject(error);\n      }\n    },\n    [Symbol.asyncIterator]() {\n      return { next };\n    }\n  };\n}\nfunction extractText(value) {\n  if (typeof value === \"string\") return value;\n  if (!isRecord3(value)) return null;\n  const direct = stringValue2(value.value) ?? stringValue2(value.text) ?? stringValue2(value.content) ?? stringValue2(value.delta) ?? stringValue2(value.message?.content);\n  if (direct) return direct;\n  const candidateText = extractCandidateText(value.candidates);\n  return candidateText || null;\n}\nfunction extractCandidateText(value) {\n  if (!Array.isArray(value)) return null;\n  const parts = value.flatMap((candidate) => {\n    if (!isRecord3(candidate)) return [];\n    const content = candidate.content;\n    if (!isRecord3(content) || !Array.isArray(content.parts)) return [];\n    return content.parts.map((part) => isRecord3(part) ? stringValue2(part.text) : void 0);\n  }).filter((text) => Boolean(text));\n  return parts.join(\"\") || null;\n}\nfunction stringValue2(value) {\n  return typeof value === \"string\" && value.length > 0 ? value : void 0;\n}\nfunction isRecord3(value) {\n  return typeof value === \"object\" && value !== null && !Array.isArray(value);\n}\n\n// packages/daemon/src/agent-adapters/local-command-runner.ts\nimport { execFile, spawn } from \"node:child_process\";\nimport { promisify } from \"node:util\";\nvar execFileAsync = promisify(execFile);\nvar localCommandRunner = {\n  async commandExists(command) {\n    try {\n      await execFileAsync(\"sh\", [\"-lc\", 'command -v \"$1\" >/dev/null 2>&1', \"sh\", command]);\n      return true;\n    } catch {\n      return false;\n    }\n  },\n  async readVersion(command, args = [\"--version\"]) {\n    try {\n      const result = await execFileAsync(command, args, { timeout: 1e4 });\n      return (result.stdout || result.stderr).trim() || void 0;\n    } catch {\n      return void 0;\n    }\n  },\n  runCommand(spec, options = {}) {\n    return new Promise((resolve2, reject) => {\n      if (options.signal?.aborted) {\n        reject(options.signal.reason ?? new Error(\"Process aborted\"));\n        return;\n      }\n      const child = spawn(spec.command, spec.args, {\n        cwd: options.cwd ?? process.env.HOME ?? process.cwd(),\n        env: process.env,\n        stdio: [\"ignore\", \"pipe\", \"pipe\"]\n      });\n      let stdout = \"\";\n      let stderr = \"\";\n      const appendStdout = (chunk) => {\n        stdout += chunk.toString();\n        options.onStdout?.(chunk);\n      };\n      const appendStderr = (chunk) => {\n        stderr += chunk.toString();\n        if (stderr.length > 8e3) stderr = stderr.slice(-8e3);\n        options.onStderr?.(chunk);\n      };\n      const abort = () => {\n        child.kill(\"SIGTERM\");\n        setTimeout(() => child.kill(\"SIGKILL\"), 1500).unref();\n      };\n      options.signal?.addEventListener(\"abort\", abort, { once: true });\n      child.stdout.on(\"data\", appendStdout);\n      child.stderr.on(\"data\", appendStderr);\n      child.on(\"error\", (error) => {\n        options.signal?.removeEventListener(\"abort\", abort);\n        reject(error);\n      });\n      child.on(\"exit\", (code) => {\n        options.signal?.removeEventListener(\"abort\", abort);\n        if (options.signal?.aborted) {\n          reject(options.signal.reason ?? new Error(\"Process aborted\"));\n          return;\n        }\n        if (code === 0) {\n          resolve2({ stdout, stderr });\n          return;\n        }\n        reject(new Error(`${spec.command} exited with code ${code ?? \"null\"}: ${(stderr || stdout).slice(-1e3)}`));\n      });\n    });\n  }\n};\n\n// packages/daemon/src/agent-adapters/opencode-adapter.ts\nimport { stat } from \"node:fs/promises\";\nimport { basename, relative as relative3 } from \"node:path\";\nimport { pathToFileURL } from \"node:url\";\nfunction buildOpenCodeServeCommand(options) {\n  return {\n    command: \"opencode\",\n    args: [\"serve\", \"--hostname\", options.hostname ?? \"127.0.0.1\", \"--port\", String(options.port)]\n  };\n}\nfunction createOpenCodeAdapter(runner, options) {\n  const port2 = options?.port ?? 4096;\n  const client = options?.client;\n  const serverManager = options?.serverManager;\n  const sessionByConversationId = /* @__PURE__ */ new Map();\n  const pendingPermissions = /* @__PURE__ */ new Map();\n  const pendingQuestions = /* @__PURE__ */ new Map();\n  async function getActiveClient() {\n    return client ?? (serverManager ? await serverManager.ensureClient() : null);\n  }\n  return {\n    backend: \"opencode\",\n    name: \"opencode\",\n    label: \"OpenCode\",\n    async probe() {\n      const exists = await runner.commandExists(\"opencode\");\n      if (!exists) return { backend: \"opencode\", state: \"missing\", detail: \"opencode command not found\" };\n      return {\n        backend: \"opencode\",\n        state: \"ready\",\n        version: await runner.readVersion?.(\"opencode\", [\"--version\"])\n      };\n    },\n    async getModelInfo() {\n      const activeClient = await getActiveClient();\n      if (!activeClient?.listModels) return null;\n      try {\n        const availableModels = await activeClient.listModels();\n        return {\n          currentModelId: null,\n          currentModelLabel: \"Default OpenCode model\",\n          availableModels,\n          canSwitch: availableModels.length > 0,\n          source: \"models\"\n        };\n      } catch {\n        return null;\n      }\n    },\n    async *sendMessage(input) {\n      const activeClient = await getActiveClient();\n      if (activeClient) {\n        const cachedSessionId = sessionByConversationId.get(input.conversationId);\n        const slashCommand = parseOpenCodeSlashCommand(input.input);\n        if (slashCommand && await isKnownOpenCodeCommand(activeClient, slashCommand.command, input.workspace)) {\n          const parts = await buildOpenCodeCommandParts(input.files);\n          const commandArguments = await appendSelectedFilesToCommandArguments(\n            slashCommand.arguments,\n            input.files,\n            input.workspace\n          );\n          const commandInput = {\n            command: slashCommand.command,\n            arguments: commandArguments,\n            sessionId: cachedSessionId,\n            directory: input.workspace,\n            model: input.model,\n            agent: input.sessionMode,\n            ...input.signal ? { signal: input.signal } : {},\n            ...parts.length ? { parts } : {}\n          };\n          if (activeClient.streamCommand) {\n            yield* streamOpenCodeEvents(activeClient.streamCommand(commandInput), input.conversationId);\n            return;\n          }\n          const result2 = await activeClient.sendCommand(commandInput);\n          sessionByConversationId.set(input.conversationId, result2.sessionId);\n          yield {\n            type: \"thought\",\n            subject: \"OpenCode\",\n            description: `session ${result2.sessionId}`\n          };\n          yield {\n            type: \"content\",\n            content: result2.text\n          };\n          return;\n        }\n        const promptFiles = await buildOpenCodePromptFiles(input.files, input.workspace);\n        const prompt = await appendSelectedFilesToPrompt3(input.input, input.files, input.workspace);\n        if (activeClient.streamPrompt) {\n          yield* streamOpenCodeEvents(\n            activeClient.streamPrompt({\n              prompt,\n              sessionId: cachedSessionId,\n              directory: input.workspace,\n              ...input.signal ? { signal: input.signal } : {},\n              ...input.model ? { model: input.model } : {},\n              ...input.sessionMode ? { agent: input.sessionMode } : {},\n              ...promptFiles.length ? { files: promptFiles } : {}\n            }),\n            input.conversationId\n          );\n          return;\n        }\n        const result = await activeClient.sendPrompt({\n          prompt,\n          sessionId: cachedSessionId,\n          directory: input.workspace,\n          ...input.signal ? { signal: input.signal } : {},\n          ...input.model ? { model: input.model } : {},\n          ...input.sessionMode ? { agent: input.sessionMode } : {},\n          ...promptFiles.length ? { files: promptFiles } : {}\n        });\n        sessionByConversationId.set(input.conversationId, result.sessionId);\n        yield {\n          type: \"thought\",\n          subject: \"OpenCode\",\n          description: `session ${result.sessionId}`\n        };\n        yield {\n          type: \"content\",\n          content: result.text\n        };\n        return;\n      }\n      const command = buildOpenCodeServeCommand({ port: port2 });\n      yield {\n        type: \"thought\",\n        subject: \"OpenCode\",\n        description: `${[command.command, ...command.args].join(\" \")} for ${input.conversationId}`\n      };\n      yield {\n        type: \"content\",\n        content: \"OpenCode server adapter is prepared; session HTTP/SSE wiring is the next runtime slice.\"\n      };\n    },\n    async getSlashCommands(input) {\n      const activeClient = await getActiveClient();\n      if (!activeClient) return [];\n      return activeClient.listCommands({ directory: input.workspace });\n    },\n    async abort(input) {\n      const sessionId = sessionByConversationId.get(input.conversationId);\n      if (!sessionId) return { success: true };\n      const activeClient = await getActiveClient();\n      if (!activeClient?.abortSession) return { success: true };\n      return activeClient.abortSession({ sessionId });\n    },\n    async confirm(input) {\n      const questionRecord = pendingQuestions.get(input.confirmationId);\n      if (questionRecord) {\n        const activeClient2 = await getActiveClient();\n        if (input.data === \"reject\") {\n          if (!activeClient2?.rejectQuestion) {\n            return {\n              success: false,\n              error: { code: \"CONFIRMATION_NOT_SUPPORTED\", message: \"OpenCode client cannot reject questions\" }\n            };\n          }\n          const result3 = await activeClient2.rejectQuestion({\n            sessionId: questionRecord.sessionId,\n            requestId: questionRecord.requestId\n          });\n          pendingQuestions.delete(input.confirmationId);\n          return result3;\n        }\n        if (!activeClient2?.replyQuestion) {\n          return {\n            success: false,\n            error: { code: \"CONFIRMATION_NOT_SUPPORTED\", message: \"OpenCode client cannot answer questions\" }\n          };\n        }\n        const answers = normalizeOpenCodeQuestionAnswers(input.data);\n        if (answers.length === 0) {\n          return {\n            success: false,\n            error: { code: \"INVALID_CONFIRMATION_REPLY\", message: \"Unsupported OpenCode question answer\" }\n          };\n        }\n        const result2 = await activeClient2.replyQuestion({\n          sessionId: questionRecord.sessionId,\n          requestId: questionRecord.requestId,\n          answers\n        });\n        pendingQuestions.delete(input.confirmationId);\n        return result2;\n      }\n      const record = pendingPermissions.get(input.confirmationId);\n      if (!record) {\n        return {\n          success: false,\n          error: {\n            code: \"CONFIRMATION_NOT_FOUND\",\n            message: `OpenCode confirmation not found: ${input.confirmationId}`\n          }\n        };\n      }\n      const reply = normalizeOpenCodePermissionReply(input.data);\n      if (!reply) {\n        return {\n          success: false,\n          error: { code: \"INVALID_CONFIRMATION_REPLY\", message: \"Unsupported OpenCode confirmation reply\" }\n        };\n      }\n      const activeClient = await getActiveClient();\n      if (!activeClient?.confirmPermission) {\n        return {\n          success: false,\n          error: { code: \"CONFIRMATION_NOT_SUPPORTED\", message: \"OpenCode client cannot confirm permissions\" }\n        };\n      }\n      const result = await activeClient.confirmPermission({\n        sessionId: record.sessionId,\n        requestId: record.requestId,\n        reply\n      });\n      pendingPermissions.delete(input.confirmationId);\n      return result;\n    }\n  };\n  async function* streamOpenCodeEvents(stream, conversationId) {\n    for await (const event of stream) {\n      switch (event.type) {\n        case \"session\":\n          sessionByConversationId.set(conversationId, event.sessionId);\n          yield {\n            type: \"thought\",\n            subject: \"OpenCode\",\n            description: `session ${event.sessionId}`\n          };\n          break;\n        case \"content\":\n          yield {\n            type: \"content\",\n            content: event.content\n          };\n          break;\n        case \"tool\":\n          yield {\n            type: \"tool_group\",\n            tools: [event.tool]\n          };\n          break;\n        case \"thinking\":\n          yield {\n            type: \"thinking\",\n            subject: event.subject ?? \"OpenCode reasoning\",\n            content: event.content,\n            status: event.status\n          };\n          break;\n        case \"context_usage\":\n          yield {\n            type: \"context_usage\",\n            used: event.used,\n            size: event.size\n          };\n          break;\n        case \"agent_status\":\n          yield {\n            type: \"agent_status\",\n            data: event.data\n          };\n          break;\n        case \"todo\":\n          yield {\n            type: \"plan\",\n            data: {\n              sessionId: event.sessionId,\n              entries: event.todos.map((todo) => ({\n                title: todo.content,\n                status: todo.status,\n                ...todo.priority ? { priority: todo.priority } : {}\n              }))\n            }\n          };\n          break;\n        case \"permission\": {\n          const confirmation = toOpenCodeConfirmation(event.request);\n          if (!confirmation) break;\n          pendingPermissions.set(confirmation.id, {\n            sessionId: event.request.sessionID,\n            requestId: event.request.id\n          });\n          yield {\n            type: \"permission\",\n            confirmation\n          };\n          break;\n        }\n        case \"permission_resolved\":\n          pendingPermissions.delete(event.requestId);\n          yield {\n            type: \"permission_resolved\",\n            confirmationId: event.requestId\n          };\n          break;\n        case \"question\": {\n          const confirmation = toOpenCodeQuestionConfirmation(event.request);\n          if (!confirmation) break;\n          pendingQuestions.set(confirmation.id, {\n            sessionId: event.request.sessionID,\n            requestId: event.request.id\n          });\n          yield {\n            type: \"permission\",\n            confirmation\n          };\n          break;\n        }\n        case \"question_resolved\":\n          pendingQuestions.delete(event.requestId);\n          yield {\n            type: \"permission_resolved\",\n            confirmationId: event.requestId\n          };\n          break;\n      }\n    }\n  }\n}\nasync function buildOpenCodePromptFiles(files = [], workspace) {\n  const attachments = [];\n  for (const filePath of files) {\n    try {\n      const fileStats = await stat(filePath);\n      if (!fileStats.isFile()) continue;\n      attachments.push({\n        uri: pathToFileURL(filePath).toString(),\n        mime: fileMimeType(filePath),\n        name: basename(filePath),\n        description: workspace ? normalizeRelativePath3(relative3(workspace, filePath)) : basename(filePath)\n      });\n    } catch {\n    }\n  }\n  return attachments;\n}\nasync function appendSelectedFilesToPrompt3(input, files = [], workspace) {\n  const selectedPaths = [];\n  for (const filePath of files) {\n    try {\n      const fileStats = await stat(filePath);\n      if (!fileStats.isFile() && !fileStats.isDirectory()) continue;\n      selectedPaths.push(workspace ? normalizeRelativePath3(relative3(workspace, filePath)) : basename(filePath));\n    } catch {\n    }\n  }\n  if (selectedPaths.length === 0) return input;\n  return `${input}\n\nSelected files:\n${selectedPaths.map((filePath) => `- ${filePath}`).join(\"\\n\")}`;\n}\nasync function appendSelectedFilesToCommandArguments(input, files = [], workspace) {\n  const output = await appendSelectedFilesToPrompt3(input, files, workspace);\n  if (input.length === 0 && output.startsWith(\"\\n\\nSelected files:\\n\")) return output.slice(2);\n  return output;\n}\nasync function isKnownOpenCodeCommand(client, command, directory) {\n  try {\n    const commands = await client.listCommands({ directory });\n    return commands.some((item) => item.command === command);\n  } catch {\n    return false;\n  }\n}\nasync function buildOpenCodeCommandParts(files = []) {\n  const parts = [];\n  for (const filePath of files) {\n    try {\n      const fileStats = await stat(filePath);\n      if (!fileStats.isFile()) continue;\n      parts.push({\n        type: \"file\",\n        mime: fileMimeType(filePath),\n        filename: basename(filePath),\n        url: pathToFileURL(filePath).toString()\n      });\n    } catch {\n    }\n  }\n  return parts;\n}\nfunction fileMimeType(path) {\n  const ext = path.toLowerCase().split(\".\").pop() ?? \"\";\n  if (ext === \"jpg\" || ext === \"jpeg\") return \"image/jpeg\";\n  if (ext === \"gif\") return \"image/gif\";\n  if (ext === \"webp\") return \"image/webp\";\n  if (ext === \"svg\") return \"image/svg+xml\";\n  if (ext === \"bmp\") return \"image/bmp\";\n  if (ext === \"avif\") return \"image/avif\";\n  if (ext === \"json\") return \"application/json\";\n  if (ext === \"html\" || ext === \"htm\") return \"text/html\";\n  if (ext === \"css\") return \"text/css\";\n  if (ext === \"csv\") return \"text/csv\";\n  if (ext === \"md\" || ext === \"markdown\") return \"text/markdown\";\n  return \"text/plain\";\n}\nfunction normalizeRelativePath3(path) {\n  return path.replace(/\\\\/g, \"/\");\n}\nfunction parseOpenCodeSlashCommand(input) {\n  const trimmed = input.trim();\n  const match = trimmed.match(/^\\/([a-zA-Z0-9_-]+)(?:\\s+(.*))?$/);\n  if (!match) return null;\n  return {\n    command: match[1],\n    arguments: match[2] ?? \"\"\n  };\n}\nfunction toOpenCodeConfirmation(request) {\n  const requestId = request.id;\n  const sessionId = request.sessionID;\n  if (!requestId || !sessionId) return null;\n  const action = typeof request.action === \"string\" && request.action ? request.action : stringValue3(request.permission) || \"tool\";\n  const resources = Array.isArray(request.resources) ? request.resources.filter((item) => typeof item === \"string\" && item.length > 0) : Array.isArray(request.patterns) ? request.patterns.filter((item) => typeof item === \"string\" && item.length > 0) : [];\n  const source = isRecord4(request.source) ? request.source : isRecord4(request.tool) ? request.tool : {};\n  const callId = stringValue3(source.callID) || requestId;\n  return {\n    id: requestId,\n    title: \"OpenCode permission\",\n    action,\n    description: openCodePermissionDescription(action, resources, request.metadata),\n    call_id: callId,\n    callId,\n    options: [\n      { label: \"Allow once\", value: \"once\" },\n      { label: \"Allow always\", value: \"always\" },\n      { label: \"Reject\", value: \"reject\" }\n    ]\n  };\n}\nfunction toOpenCodeQuestionConfirmation(request) {\n  const requestId = request.id;\n  const sessionId = request.sessionID;\n  if (!requestId || !sessionId) return null;\n  const question = Array.isArray(request.questions) ? request.questions.find(isRecord4) : void 0;\n  const header = question ? stringValue3(question.header) : void 0;\n  const prompt = question ? stringValue3(question.question) : void 0;\n  const description = [header, prompt].filter(Boolean).join(\"\\n\") || \"OpenCode question\";\n  const source = isRecord4(request.tool) ? request.tool : isRecord4(request.source) ? request.source : {};\n  const callId = stringValue3(source.callID) || requestId;\n  return {\n    id: requestId,\n    title: \"OpenCode question\",\n    action: \"question\",\n    description,\n    call_id: callId,\n    callId,\n    command_type: \"question\",\n    questions: openCodeQuestions(request.questions),\n    options: [...openCodeQuestionOptions(question), { label: \"Reject\", value: \"reject\" }]\n  };\n}\nfunction openCodeQuestionOptions(question) {\n  const rawOptions = Array.isArray(question?.options) ? question.options : [];\n  return rawOptions.map((option) => {\n    const label = typeof option === \"string\" ? option : isRecord4(option) ? stringValue3(option.label) : void 0;\n    return label ? { label, value: label } : null;\n  }).filter((option) => option !== null);\n}\nfunction openCodeQuestions(questions) {\n  if (!Array.isArray(questions)) return [];\n  return questions.filter(isRecord4).map((question) => {\n    const header = stringValue3(question.header);\n    const prompt = stringValue3(question.question) || \"OpenCode question\";\n    return {\n      ...header ? { header } : {},\n      question: prompt,\n      multiple: question.multiple === true,\n      custom: question.custom !== false,\n      options: openCodeQuestionOptionsWithDescriptions(question)\n    };\n  });\n}\nfunction openCodeQuestionOptionsWithDescriptions(question) {\n  const rawOptions = Array.isArray(question.options) ? question.options : [];\n  return rawOptions.map((option) => {\n    const label = typeof option === \"string\" ? option : isRecord4(option) ? stringValue3(option.label) : void 0;\n    if (!label) return null;\n    const description = isRecord4(option) ? stringValue3(option.description) : void 0;\n    return {\n      label,\n      value: label,\n      ...description ? { description } : {}\n    };\n  }).filter((option) => option !== null);\n}\nfunction openCodePermissionDescription(action, resources, metadata) {\n  const lines = [`Action: ${action}`];\n  if (resources.length) lines.push(\"Resources:\", ...resources.map((item) => `- ${item}`));\n  if (isRecord4(metadata) && Object.keys(metadata).length) {\n    lines.push(`Metadata: ${JSON.stringify(metadata)}`);\n  }\n  return lines.join(\"\\n\");\n}\nfunction normalizeOpenCodePermissionReply(value) {\n  if (value === \"once\" || value === \"always\" || value === \"reject\") return value;\n  if (value === \"proceed_once\" || value === \"allow\" || value === \"approve\" || value === \"yes\") return \"once\";\n  if (value === \"proceed_always\" || value === \"proceed_always_tool\" || value === \"proceed_always_server\") {\n    return \"always\";\n  }\n  if (value === \"cancel\" || value === \"deny\" || value === \"no\") return \"reject\";\n  return null;\n}\nfunction normalizeOpenCodeQuestionAnswers(value) {\n  if (isQuestionAnswerList(value)) {\n    return value.map((answer) => sanitizeQuestionAnswer(answer)).filter((answer) => answer.length > 0);\n  }\n  if (Array.isArray(value)) {\n    const answer = sanitizeQuestionAnswer(value);\n    return answer.length > 0 ? [answer] : [];\n  }\n  return typeof value === \"string\" && value.length > 0 ? [[value]] : [];\n}\nfunction isQuestionAnswerList(value) {\n  return Array.isArray(value) && value.every((item) => Array.isArray(item));\n}\nfunction sanitizeQuestionAnswer(value) {\n  return value.filter((item) => typeof item === \"string\" && item.length > 0);\n}\nfunction isRecord4(value) {\n  return typeof value === \"object\" && value !== null && !Array.isArray(value);\n}\nfunction stringValue3(value) {\n  return typeof value === \"string\" && value.length > 0 ? value : void 0;\n}\n\n// packages/daemon/src/agent-adapters/opencode-server.ts\nimport { spawn as spawn2 } from \"node:child_process\";\n\n// packages/daemon/src/agent-adapters/opencode-client.ts\nfunction createOpenCodeClient(options) {\n  const fetchImpl = options.fetch ?? fetch;\n  const baseUrl = options.baseUrl.replace(/\\/+$/, \"\");\n  async function requestJson(path, init) {\n    const response = await fetchImpl(`${baseUrl}${path}`, {\n      ...init,\n      headers: {\n        ...init?.body ? { \"Content-Type\": \"application/json\" } : {},\n        ...init?.headers\n      }\n    });\n    if (!response.ok) {\n      const body = await response.text().catch(() => \"\");\n      throw new Error(`OpenCode ${init?.method ?? \"GET\"} ${path} failed (${response.status}): ${body}`);\n    }\n    if (response.status === 204) {\n      return void 0;\n    }\n    return await response.json();\n  }\n  async function createSession(input) {\n    const location = {\n      ...input.directory ? { directory: input.directory } : {},\n      ...input.workspace ? { workspace: input.workspace } : {}\n    };\n    const model = parseOpenCodeModelRef(input.model);\n    const response = await requestJson(\"/api/session\", {\n      method: \"POST\",\n      signal: input.signal,\n      body: JSON.stringify({\n        ...Object.keys(location).length ? { location } : {},\n        ...model ? { model } : {},\n        ...input.agent ? { agent: input.agent } : {}\n      })\n    });\n    const id = response.data?.id;\n    if (typeof id !== \"string\") {\n      throw new Error(\"OpenCode session.create returned no session id\");\n    }\n    return id;\n  }\n  async function* streamTurn(input, submitTurn) {\n    const sessionId = input.sessionId ?? await createSession(input);\n    yield { type: \"session\", sessionId };\n    const queue = createAsyncQueue3();\n    let streamedContent = false;\n    const eventStream = subscribeOpenCodeSessionEvents({\n      baseUrl,\n      fetchImpl,\n      sessionId,\n      directory: input.directory,\n      workspace: input.workspace,\n      signal: input.signal,\n      handlers: {\n        onContent(content) {\n          streamedContent = true;\n          queue.push({ type: \"content\", content });\n        },\n        onTool(tool) {\n          queue.push({ type: \"tool\", tool });\n        },\n        onThinking(update) {\n          queue.push({ type: \"thinking\", ...update });\n        },\n        onContextUsage(update) {\n          queue.push({ type: \"context_usage\", ...update });\n        },\n        onAgentStatus(update) {\n          queue.push({ type: \"agent_status\", data: update.data });\n        },\n        onTodo(update) {\n          queue.push({ type: \"todo\", ...update });\n        },\n        onPermission(request) {\n          queue.push({ type: \"permission\", request });\n        },\n        onPermissionResolved(requestId) {\n          queue.push({ type: \"permission_resolved\", requestId });\n        },\n        onQuestion(request) {\n          queue.push({ type: \"question\", request });\n        },\n        onQuestionResolved(requestId) {\n          queue.push({ type: \"question_resolved\", requestId });\n        }\n      }\n    });\n    let requestTask;\n    try {\n      await eventStream.ready;\n      requestTask = (async () => {\n        await submitTurn(sessionId);\n        await requestJson(`/api/session/${encodeURIComponent(sessionId)}/wait`, {\n          method: \"POST\",\n          signal: input.signal\n        });\n      })();\n      requestTask.then(\n        async () => {\n          eventStream.close();\n          await eventStream.done;\n          queue.close();\n        },\n        (error) => {\n          eventStream.close();\n          queue.fail(error);\n        }\n      );\n      for await (const event of queue) {\n        yield event;\n      }\n      await requestTask;\n    } finally {\n      eventStream.close();\n      await eventStream.done.catch(() => {\n      });\n    }\n    const context = await requestJson(`/api/session/${encodeURIComponent(sessionId)}/context`, {\n      method: \"GET\",\n      signal: input.signal\n    });\n    const text = extractOpenCodeAssistantText(context.data ?? []);\n    if (!streamedContent && text) {\n      yield { type: \"content\", content: text };\n    }\n  }\n  return {\n    async sendPrompt(input) {\n      const sessionId = input.sessionId ?? await createSession(input);\n      await requestJson(`/api/session/${encodeURIComponent(sessionId)}/prompt`, {\n        method: \"POST\",\n        signal: input.signal,\n        body: JSON.stringify({\n          prompt: {\n            text: input.prompt,\n            files: input.files ?? [],\n            agents: []\n          }\n        })\n      });\n      await requestJson(`/api/session/${encodeURIComponent(sessionId)}/wait`, { method: \"POST\", signal: input.signal });\n      const context = await requestJson(\n        `/api/session/${encodeURIComponent(sessionId)}/context`,\n        { method: \"GET\", signal: input.signal }\n      );\n      return {\n        sessionId,\n        text: extractOpenCodeAssistantText(context.data ?? [])\n      };\n    },\n    async sendCommand(input) {\n      const sessionId = input.sessionId ?? await createSession(input);\n      await requestJson(commandSendPath(sessionId, input), {\n        method: \"POST\",\n        signal: input.signal,\n        body: JSON.stringify({\n          command: input.command,\n          arguments: input.arguments ?? \"\",\n          ...input.messageId ? { messageID: input.messageId } : {},\n          ...input.model ? { model: input.model } : {},\n          ...input.agent ? { agent: input.agent } : {},\n          ...input.parts?.length ? { parts: input.parts } : {}\n        })\n      });\n      await requestJson(`/api/session/${encodeURIComponent(sessionId)}/wait`, { method: \"POST\", signal: input.signal });\n      const context = await requestJson(\n        `/api/session/${encodeURIComponent(sessionId)}/context`,\n        { method: \"GET\", signal: input.signal }\n      );\n      return {\n        sessionId,\n        text: extractOpenCodeAssistantText(context.data ?? [])\n      };\n    },\n    streamPrompt(input) {\n      return streamTurn(input, async (sessionId) => {\n        await requestJson(`/api/session/${encodeURIComponent(sessionId)}/prompt`, {\n          method: \"POST\",\n          signal: input.signal,\n          body: JSON.stringify({\n            prompt: {\n              text: input.prompt,\n              files: input.files ?? [],\n              agents: []\n            }\n          })\n        });\n      });\n    },\n    streamCommand(input) {\n      return streamTurn(input, async (sessionId) => {\n        await requestJson(commandSendPath(sessionId, input), {\n          method: \"POST\",\n          signal: input.signal,\n          body: JSON.stringify({\n            command: input.command,\n            arguments: input.arguments ?? \"\",\n            ...input.messageId ? { messageID: input.messageId } : {},\n            ...input.model ? { model: input.model } : {},\n            ...input.agent ? { agent: input.agent } : {},\n            ...input.parts?.length ? { parts: input.parts } : {}\n          })\n        });\n      });\n    },\n    async confirmPermission(input) {\n      try {\n        await requestJson(`/api/session/${encodeURIComponent(input.sessionId)}/permission/${encodeURIComponent(input.requestId)}/reply`, {\n          method: \"POST\",\n          body: JSON.stringify({ reply: input.reply })\n        });\n      } catch {\n        await requestJson(`/api/session/${encodeURIComponent(input.sessionId)}/permissions/${encodeURIComponent(input.requestId)}`, {\n          method: \"POST\",\n          body: JSON.stringify({ response: input.reply })\n        });\n      }\n      return { success: true };\n    },\n    async replyQuestion(input) {\n      await requestJson(`/api/session/${encodeURIComponent(input.sessionId)}/question/${encodeURIComponent(input.requestId)}/reply`, {\n        method: \"POST\",\n        body: JSON.stringify({ answers: input.answers })\n      });\n      return { success: true };\n    },\n    async rejectQuestion(input) {\n      await requestJson(`/api/session/${encodeURIComponent(input.sessionId)}/question/${encodeURIComponent(input.requestId)}/reject`, {\n        method: \"POST\"\n      });\n      return { success: true };\n    },\n    async abortSession(input) {\n      await requestJson(`/session/${encodeURIComponent(input.sessionId)}/abort`, { method: \"POST\" });\n      return { success: true };\n    },\n    async listCommands(input = {}) {\n      const v2Path = commandListV2Path(input);\n      try {\n        const response = await requestJson(v2Path, { method: \"GET\" });\n        return extractOpenCodeCommands(response);\n      } catch {\n        const legacyPath = commandListLegacyPath(input);\n        const response = await requestJson(legacyPath, { method: \"GET\" });\n        return extractOpenCodeCommands(response);\n      }\n    },\n    async listModels() {\n      const response = await requestJson(\"/api/model\", { method: \"GET\" });\n      return extractOpenCodeModels(response);\n    }\n  };\n}\nfunction createAsyncQueue3() {\n  const values = [];\n  const waiters = [];\n  let closed = false;\n  let failure;\n  const next = () => {\n    if (values.length > 0) {\n      return Promise.resolve({ value: values.shift(), done: false });\n    }\n    if (failure !== void 0) {\n      return Promise.reject(failure);\n    }\n    if (closed) {\n      return Promise.resolve({ value: void 0, done: true });\n    }\n    return new Promise((resolve2, reject) => {\n      waiters.push({ resolve: resolve2, reject });\n    });\n  };\n  return {\n    push(value) {\n      if (closed || failure !== void 0) return;\n      const waiter = waiters.shift();\n      if (waiter) {\n        waiter.resolve({ value, done: false });\n        return;\n      }\n      values.push(value);\n    },\n    close() {\n      if (closed) return;\n      closed = true;\n      for (const waiter of waiters.splice(0)) {\n        waiter.resolve({ value: void 0, done: true });\n      }\n    },\n    fail(error) {\n      if (closed || failure !== void 0) return;\n      failure = error;\n      for (const waiter of waiters.splice(0)) {\n        waiter.reject(error);\n      }\n    },\n    [Symbol.asyncIterator]() {\n      return { next };\n    }\n  };\n}\nfunction subscribeOpenCodeSessionEvents(input) {\n  const controller = new AbortController();\n  const abortFromParent = () => controller.abort(input.signal?.reason ?? new Error(\"OpenCode event stream aborted\"));\n  if (input.signal?.aborted) abortFromParent();\n  else input.signal?.addEventListener(\"abort\", abortFromParent, { once: true });\n  let resolveReady = () => {\n  };\n  const ready = new Promise((resolve2) => {\n    resolveReady = resolve2;\n  });\n  const done = (async () => {\n    try {\n      const response = await input.fetchImpl(`${input.baseUrl}${eventStreamPath(input)}`, {\n        method: \"GET\",\n        signal: controller.signal\n      });\n      resolveReady();\n      if (!response.ok || !response.body) return;\n      const reader = response.body.getReader();\n      const decoder = new TextDecoder();\n      const extractTextDelta = createOpenCodeTextDeltaExtractor(input.sessionId);\n      const extractReasoningEvent = createOpenCodeReasoningEventExtractor(input.sessionId);\n      const extractCompactionEvent = createOpenCodeCompactionEventExtractor(input.sessionId);\n      const extractToolUpdate = createOpenCodeToolUpdateExtractor(input.sessionId);\n      const extractContextUsage = createOpenCodeContextUsageExtractor(input.sessionId);\n      const extractAgentStatus = createOpenCodeAgentStatusExtractor(input.sessionId);\n      const extractTodoEvent = createOpenCodeTodoEventExtractor(input.sessionId);\n      const extractPermissionEvent = createOpenCodePermissionEventExtractor(input.sessionId);\n      const extractQuestionEvent = createOpenCodeQuestionEventExtractor(input.sessionId);\n      const parse = createSseParser((event) => {\n        const text = extractTextDelta(event);\n        if (text) input.handlers.onContent(text);\n        for (const reasoning of extractReasoningEvent(event)) {\n          input.handlers.onThinking(reasoning);\n        }\n        const compaction = extractCompactionEvent(event);\n        if (compaction) input.handlers.onThinking(compaction);\n        const tool = extractToolUpdate(event);\n        if (tool) input.handlers.onTool(tool);\n        const contextUsage = extractContextUsage(event);\n        if (contextUsage) input.handlers.onContextUsage(contextUsage);\n        const agentStatus = extractAgentStatus(event);\n        if (agentStatus) input.handlers.onAgentStatus(agentStatus);\n        const todoEvent = extractTodoEvent(event);\n        if (todoEvent) input.handlers.onTodo(todoEvent);\n        const permissionEvent = extractPermissionEvent(event);\n        if (permissionEvent?.type === \"asked\") input.handlers.onPermission(permissionEvent.request);\n        if (permissionEvent?.type === \"resolved\") input.handlers.onPermissionResolved(permissionEvent.requestId);\n        const questionEvent = extractQuestionEvent(event);\n        if (questionEvent?.type === \"asked\") input.handlers.onQuestion(questionEvent.request);\n        if (questionEvent?.type === \"resolved\") input.handlers.onQuestionResolved(questionEvent.requestId);\n      });\n      while (true) {\n        const next = await reader.read();\n        if (next.done) break;\n        parse(decoder.decode(next.value, { stream: true }));\n      }\n      parse(decoder.decode());\n    } catch {\n      resolveReady();\n    } finally {\n      resolveReady();\n      input.signal?.removeEventListener(\"abort\", abortFromParent);\n    }\n  })();\n  return {\n    ready,\n    done,\n    close() {\n      controller.abort(new Error(\"OpenCode event stream closed\"));\n    }\n  };\n}\nfunction eventStreamPath(input) {\n  if (input.directory) return `/api/event?location%5Bdirectory%5D=${encodeURIComponent(input.directory)}`;\n  if (input.workspace) return `/api/event?location%5Bworkspace%5D=${encodeURIComponent(input.workspace)}`;\n  return \"/api/event\";\n}\nfunction createSseParser(onEvent) {\n  let buffer = \"\";\n  return (chunk) => {\n    buffer += chunk;\n    const blocks = buffer.split(/\\r?\\n\\r?\\n/);\n    buffer = blocks.pop() || \"\";\n    for (const block of blocks) parseSseEventBlock(block, onEvent);\n  };\n}\nfunction parseSseEventBlock(block, onEvent) {\n  const data = block.split(/\\r?\\n/).filter((line) => line.startsWith(\"data:\")).map((line) => line.slice(\"data:\".length).trimStart()).join(\"\\n\");\n  if (!data) return;\n  try {\n    onEvent(JSON.parse(data));\n  } catch {\n  }\n}\nfunction createOpenCodeTextDeltaExtractor(sessionId) {\n  const textByPartId = /* @__PURE__ */ new Map();\n  return (event) => extractOpenCodeEventTextDelta(event, sessionId, textByPartId);\n}\nfunction extractOpenCodeEventTextDelta(event, sessionId, textByPartId) {\n  if (!isRecord5(event)) return \"\";\n  const payload = isRecord5(event.payload) ? event.payload : event;\n  const type = typeof payload.type === \"string\" ? payload.type : \"\";\n  const data = isRecord5(payload.data) ? payload.data : isRecord5(payload.properties) ? payload.properties : {};\n  if (type === \"session.next.text.delta\" && data.sessionID === sessionId && typeof data.delta === \"string\") {\n    const textId = stringValue4(data.textID);\n    if (textId) {\n      textByPartId.set(textId, (textByPartId.get(textId) || \"\") + data.delta);\n    }\n    return data.delta;\n  }\n  if (type === \"session.next.text.ended\" && data.sessionID === sessionId && typeof data.text === \"string\") {\n    const textId = stringValue4(data.textID);\n    if (!textId) return data.text;\n    const previous2 = textByPartId.get(textId) || \"\";\n    textByPartId.set(textId, data.text);\n    return data.text.startsWith(previous2) ? data.text.slice(previous2.length) : data.text;\n  }\n  if (type === \"session.next.synthetic\" && data.sessionID === sessionId && typeof data.text === \"string\") {\n    return data.text;\n  }\n  if (type !== \"message.part.updated\") return \"\";\n  const part = isRecord5(data.part) ? data.part : {};\n  const partSessionId = typeof data.sessionID === \"string\" ? data.sessionID : part.sessionID;\n  if (partSessionId !== sessionId || part.type !== \"text\") return \"\";\n  if (typeof data.delta === \"string\") return data.delta;\n  const partId = typeof part.id === \"string\" ? part.id : \"\";\n  if (!partId || typeof part.text !== \"string\") return \"\";\n  const previous = textByPartId.get(partId) || \"\";\n  textByPartId.set(partId, part.text);\n  return part.text.startsWith(previous) ? part.text.slice(previous.length) : \"\";\n}\nfunction createOpenCodeReasoningEventExtractor(sessionId) {\n  const reasoningByPartId = /* @__PURE__ */ new Map();\n  return (event) => extractOpenCodeReasoningEvent(event, sessionId, reasoningByPartId);\n}\nfunction extractOpenCodeReasoningEvent(event, sessionId, reasoningByPartId) {\n  if (!isRecord5(event)) return [];\n  const payload = isRecord5(event.payload) ? event.payload : event;\n  const type = typeof payload.type === \"string\" ? payload.type : \"\";\n  const data = isRecord5(payload.data) ? payload.data : isRecord5(payload.properties) ? payload.properties : {};\n  if (data.sessionID !== sessionId) return [];\n  if (type === \"session.next.reasoning.delta\" && typeof data.delta === \"string\") {\n    const reasoningId = stringValue4(data.reasoningID);\n    if (reasoningId) {\n      reasoningByPartId.set(reasoningId, (reasoningByPartId.get(reasoningId) || \"\") + data.delta);\n    }\n    return [{ content: data.delta, status: \"thinking\" }];\n  }\n  if (type === \"session.next.reasoning.ended\") {\n    const updates = [];\n    if (typeof data.text === \"string\") {\n      const reasoningId = stringValue4(data.reasoningID);\n      const previous = reasoningId ? reasoningByPartId.get(reasoningId) || \"\" : \"\";\n      if (reasoningId) reasoningByPartId.set(reasoningId, data.text);\n      const suffix = data.text.startsWith(previous) ? data.text.slice(previous.length) : data.text;\n      if (suffix) updates.push({ content: suffix, status: \"thinking\" });\n    }\n    updates.push({ content: \"\", status: \"done\" });\n    return updates;\n  }\n  return [];\n}\nfunction createOpenCodeCompactionEventExtractor(sessionId) {\n  return (event) => extractOpenCodeCompactionEvent(event, sessionId);\n}\nfunction extractOpenCodeCompactionEvent(event, sessionId) {\n  if (!isRecord5(event)) return null;\n  const payload = isRecord5(event.payload) ? event.payload : event;\n  const type = typeof payload.type === \"string\" ? payload.type : \"\";\n  const data = isRecord5(payload.data) ? payload.data : isRecord5(payload.properties) ? payload.properties : {};\n  if (data.sessionID !== sessionId) return null;\n  if (type === \"session.next.compaction.started\") {\n    return { subject: \"OpenCode compaction\", content: \"Compacting context\", status: \"thinking\" };\n  }\n  if (type === \"session.next.compaction.delta\" && typeof data.text === \"string\") {\n    return { subject: \"OpenCode compaction\", content: data.text, status: \"thinking\" };\n  }\n  if (type === \"session.next.compaction.ended\") {\n    return { subject: \"OpenCode compaction\", content: \"\", status: \"done\" };\n  }\n  return null;\n}\nfunction createOpenCodeContextUsageExtractor(sessionId) {\n  return (event) => extractOpenCodeContextUsage(event, sessionId);\n}\nfunction extractOpenCodeContextUsage(event, sessionId) {\n  if (!isRecord5(event)) return null;\n  const payload = isRecord5(event.payload) ? event.payload : event;\n  const type = typeof payload.type === \"string\" ? payload.type : \"\";\n  const data = isRecord5(payload.data) ? payload.data : isRecord5(payload.properties) ? payload.properties : {};\n  if (type !== \"session.next.step.ended\") return null;\n  if (data.sessionID !== sessionId) return null;\n  const tokens = isRecord5(data.tokens) ? data.tokens : null;\n  return tokens ? openCodeContextUsageFromTokens(tokens) : null;\n}\nfunction openCodeContextUsageFromTokens(tokens) {\n  const cache = isRecord5(tokens.cache) ? tokens.cache : {};\n  const used = numberValue(tokens.input) + numberValue(tokens.output) + numberValue(tokens.reasoning) + numberValue(cache.read) + numberValue(cache.write);\n  return used > 0 ? { used, size: used } : null;\n}\nfunction createOpenCodeTodoEventExtractor(sessionId) {\n  return (event) => extractOpenCodeTodoEvent(event, sessionId);\n}\nfunction extractOpenCodeTodoEvent(event, sessionId) {\n  if (!isRecord5(event)) return null;\n  const payload = isRecord5(event.payload) ? event.payload : event;\n  const type = typeof payload.type === \"string\" ? payload.type : \"\";\n  const data = isRecord5(payload.data) ? payload.data : isRecord5(payload.properties) ? payload.properties : {};\n  if (type !== \"todo.updated\") return null;\n  return openCodeTodoPlan(data, sessionId);\n}\nfunction openCodeTodoPlan(data, sessionId) {\n  if (data.sessionID !== sessionId) return null;\n  const todos = Array.isArray(data.todos) ? data.todos.filter(isRecord5) : [];\n  return {\n    sessionId,\n    todos: todos.map((todo) => {\n      const content = stringValue4(todo.content);\n      const status = stringValue4(todo.status);\n      if (!content || !status) return null;\n      const priority = stringValue4(todo.priority);\n      return {\n        content,\n        status,\n        ...priority ? { priority } : {}\n      };\n    }).filter((todo) => Boolean(todo))\n  };\n}\nfunction createOpenCodeAgentStatusExtractor(sessionId) {\n  return (event) => extractOpenCodeAgentStatus(event, sessionId);\n}\nfunction extractOpenCodeAgentStatus(event, sessionId) {\n  if (!isRecord5(event)) return null;\n  const payload = isRecord5(event.payload) ? event.payload : event;\n  const type = typeof payload.type === \"string\" ? payload.type : \"\";\n  const data = isRecord5(payload.data) ? payload.data : isRecord5(payload.properties) ? payload.properties : {};\n  if (data.sessionID !== sessionId) return null;\n  if (type === \"session.next.agent.switched\") return openCodeAgentSwitchedStatus(data, sessionId);\n  if (type === \"session.next.model.switched\") return openCodeModelSwitchedStatus(data, sessionId);\n  if (type === \"session.next.moved\") return openCodeMovedStatus(data, sessionId);\n  if (type === \"session.next.context.updated\") return openCodeContextUpdatedStatus(data, sessionId);\n  if (type === \"session.next.step.started\") return openCodeStepStartedStatus(data, sessionId);\n  if (type === \"session.next.step.failed\") return openCodeStepFailedStatus(data, sessionId);\n  if (type === \"session.next.retried\") return openCodeRetriedStatus(data, sessionId);\n  if (type === \"session.next.prompted\") return openCodePromptedStatus(data, sessionId);\n  if (type === \"session.next.prompt.admitted\") return openCodePromptAdmittedStatus(data, sessionId);\n  if (type === \"session.next.prompt.promoted\") return openCodePromptPromotedStatus(data, sessionId);\n  if (type === \"session.next.interrupt.requested\") return openCodeInterruptRequestedStatus(sessionId);\n  return null;\n}\nfunction openCodeAgentSwitchedStatus(data, sessionId) {\n  const messageId = stringValue4(data.messageID);\n  const agent = stringValue4(data.agent);\n  const message = agent ? `Agent switched to ${agent}` : \"Agent switched\";\n  return {\n    data: {\n      ...openCodeAgentStatusBase(\"agent_switched\", sessionId),\n      ...messageId ? { messageId } : {},\n      ...agent ? { agent } : {},\n      message,\n      detail: message\n    }\n  };\n}\nfunction openCodeModelSwitchedStatus(data, sessionId) {\n  const messageId = stringValue4(data.messageID);\n  const model = openCodeModelRef(data.model);\n  const message = model ? `Model switched to ${model}` : \"Model switched\";\n  return {\n    data: {\n      ...openCodeAgentStatusBase(\"model_switched\", sessionId),\n      ...messageId ? { messageId } : {},\n      ...model ? { model } : {},\n      message,\n      detail: message\n    }\n  };\n}\nfunction openCodeMovedStatus(data, sessionId) {\n  const location = isRecord5(data.location) ? data.location : void 0;\n  const directory = location ? stringValue4(location.directory) : void 0;\n  const subdirectory = stringValue4(data.subdirectory);\n  const target = openCodeMovedTarget(directory, subdirectory);\n  const message = target ? `Workspace moved to ${target}` : \"Workspace moved\";\n  return {\n    data: {\n      ...openCodeAgentStatusBase(\"workspace_moved\", sessionId),\n      ...location ? { location } : {},\n      ...directory ? { directory } : {},\n      ...subdirectory ? { subdirectory } : {},\n      message,\n      detail: message\n    }\n  };\n}\nfunction openCodeContextUpdatedStatus(data, sessionId) {\n  const messageId = stringValue4(data.messageID);\n  const message = stringValue4(data.text) ?? \"Context updated\";\n  return {\n    data: {\n      ...openCodeAgentStatusBase(\"context_updated\", sessionId),\n      ...messageId ? { messageId } : {},\n      message,\n      detail: message\n    }\n  };\n}\nfunction openCodeStepStartedStatus(data, sessionId) {\n  const messageId = stringValue4(data.assistantMessageID);\n  const agent = stringValue4(data.agent);\n  const model = openCodeModelRef(data.model);\n  return {\n    data: {\n      ...openCodeAgentStatusBase(\"session_active\", sessionId),\n      ...messageId ? { messageId } : {},\n      ...agent ? { agent } : {},\n      ...model ? { model } : {}\n    }\n  };\n}\nfunction openCodeStepFailedStatus(data, sessionId) {\n  const messageId = stringValue4(data.assistantMessageID);\n  const error = isRecord5(data.error) ? data.error : {};\n  const message = stringValue4(error.message) ?? \"OpenCode step failed\";\n  return {\n    data: {\n      ...openCodeAgentStatusBase(\"error\", sessionId),\n      ...messageId ? { messageId } : {},\n      message,\n      detail: message\n    }\n  };\n}\nfunction openCodeRetriedStatus(data, sessionId) {\n  const attempt = typeof data.attempt === \"number\" && Number.isFinite(data.attempt) ? data.attempt : void 0;\n  const error = isRecord5(data.error) ? data.error : {};\n  const errorMessage = stringValue4(error.message);\n  const message = `Retrying OpenCode request${attempt ? ` (attempt ${attempt})` : \"\"}${errorMessage ? `: ${errorMessage}` : \"\"}`;\n  const statusCode = typeof error.statusCode === \"number\" && Number.isFinite(error.statusCode) ? error.statusCode : void 0;\n  const retryable = typeof error.isRetryable === \"boolean\" ? error.isRetryable : void 0;\n  return {\n    data: {\n      ...openCodeAgentStatusBase(\"retrying\", sessionId),\n      ...attempt !== void 0 ? { attempt } : {},\n      message,\n      detail: message,\n      ...retryable !== void 0 ? { retryable } : {},\n      ...statusCode !== void 0 ? { statusCode } : {}\n    }\n  };\n}\nfunction openCodePromptedStatus(data, sessionId) {\n  const messageId = stringValue4(data.messageID);\n  const delivery = stringValue4(data.delivery);\n  const status = delivery === \"queue\" ? \"prompt_queued\" : \"prompt_requested\";\n  const message = delivery === \"queue\" ? \"Prompt queued\" : \"Prompt requested\";\n  return {\n    data: {\n      ...openCodeAgentStatusBase(status, sessionId),\n      ...messageId ? { messageId } : {},\n      ...delivery ? { delivery } : {},\n      message,\n      detail: message\n    }\n  };\n}\nfunction openCodePromptAdmittedStatus(data, sessionId) {\n  const messageId = stringValue4(data.messageID);\n  const delivery = stringValue4(data.delivery);\n  const message = \"Prompt admitted\";\n  return {\n    data: {\n      ...openCodeAgentStatusBase(\"prompt_admitted\", sessionId),\n      ...messageId ? { messageId } : {},\n      ...delivery ? { delivery } : {},\n      message,\n      detail: message\n    }\n  };\n}\nfunction openCodePromptPromotedStatus(data, sessionId) {\n  const messageId = stringValue4(data.messageID);\n  const timeCreated = stringValue4(data.timeCreated);\n  const message = \"Queued prompt promoted\";\n  return {\n    data: {\n      ...openCodeAgentStatusBase(\"prompt_promoted\", sessionId),\n      ...messageId ? { messageId } : {},\n      ...timeCreated ? { timeCreated } : {},\n      message,\n      detail: message\n    }\n  };\n}\nfunction openCodeInterruptRequestedStatus(sessionId) {\n  const message = \"Interrupt requested\";\n  return {\n    data: {\n      ...openCodeAgentStatusBase(\"interrupt_requested\", sessionId),\n      message,\n      detail: message\n    }\n  };\n}\nfunction openCodeAgentStatusBase(status, sessionId) {\n  return {\n    backend: \"opencode\",\n    agentName: \"OpenCode\",\n    agent_name: \"OpenCode\",\n    status,\n    sessionId\n  };\n}\nfunction openCodeMovedTarget(directory, subdirectory) {\n  if (!directory) return subdirectory ?? \"\";\n  if (!subdirectory) return directory;\n  return `${directory.replace(/\\/+$/, \"\")}/${subdirectory.replace(/^\\/+/, \"\")}`;\n}\nfunction openCodeModelRef(value) {\n  if (typeof value === \"string\" && value.length > 0) return value;\n  if (!isRecord5(value)) return void 0;\n  const providerId = stringValue4(value.providerID) ?? stringValue4(value.providerId) ?? stringValue4(value.provider);\n  const id = stringValue4(value.id);\n  return providerId && id ? `${providerId}/${id}` : id;\n}\nfunction createOpenCodeToolUpdateExtractor(sessionId) {\n  const toolByCallId = /* @__PURE__ */ new Map();\n  return (event) => extractOpenCodeToolUpdate(event, sessionId, toolByCallId);\n}\nfunction extractOpenCodeToolUpdate(event, sessionId, toolByCallId) {\n  const shellTool = extractOpenCodeShellToolUpdate(event, sessionId, toolByCallId);\n  if (shellTool) return shellTool;\n  const nextTool = extractOpenCodeNextToolUpdate(event, sessionId, toolByCallId);\n  if (nextTool) return nextTool;\n  if (!isRecord5(event)) return null;\n  const payload = isRecord5(event.payload) ? event.payload : event;\n  const type = typeof payload.type === \"string\" ? payload.type : \"\";\n  const data = isRecord5(payload.data) ? payload.data : isRecord5(payload.properties) ? payload.properties : {};\n  if (type !== \"message.part.updated\") return null;\n  const part = isRecord5(data.part) ? data.part : {};\n  const partSessionId = typeof data.sessionID === \"string\" ? data.sessionID : part.sessionID;\n  if (partSessionId !== sessionId || part.type !== \"tool\") return null;\n  const state = isRecord5(part.state) ? part.state : {};\n  const callId = typeof part.callID === \"string\" ? part.callID : typeof part.id === \"string\" ? part.id : \"\";\n  if (!callId) return null;\n  const resultDisplay = openCodeToolResultDisplay(state);\n  const name = typeof part.tool === \"string\" && part.tool ? part.tool : \"tool\";\n  const description = openCodeToolTitle(part, state);\n  toolByCallId.set(callId, { name, description });\n  return openCodeToolUpdate(callId, name, description, openCodeToolStatus(state.status), resultDisplay);\n}\nfunction extractOpenCodeShellToolUpdate(event, sessionId, toolByCallId) {\n  if (!isRecord5(event)) return null;\n  const payload = isRecord5(event.payload) ? event.payload : event;\n  const type = typeof payload.type === \"string\" ? payload.type : \"\";\n  const data = isRecord5(payload.data) ? payload.data : isRecord5(payload.properties) ? payload.properties : {};\n  if (data.sessionID !== sessionId) return null;\n  const callId = stringValue4(data.callID);\n  if (!callId) return null;\n  if (type === \"session.next.shell.started\") {\n    const command = stringValue4(data.command) ?? \"shell\";\n    toolByCallId.set(callId, { name: \"shell\", description: command });\n    return openCodeToolUpdate(callId, \"shell\", command, \"Executing\", \"\");\n  }\n  if (type === \"session.next.shell.ended\") {\n    const cached = toolByCallId.get(callId) ?? { name: \"shell\", description: callId };\n    const output = typeof data.output === \"string\" ? data.output : \"\";\n    return openCodeToolUpdate(callId, cached.name, cached.description, \"Success\", output);\n  }\n  return null;\n}\nfunction extractOpenCodeNextToolUpdate(event, sessionId, toolByCallId) {\n  if (!isRecord5(event)) return null;\n  const payload = isRecord5(event.payload) ? event.payload : event;\n  const type = typeof payload.type === \"string\" ? payload.type : \"\";\n  const data = isRecord5(payload.data) ? payload.data : isRecord5(payload.properties) ? payload.properties : {};\n  if (data.sessionID !== sessionId) return null;\n  const callId = stringValue4(data.callID);\n  if (!callId) return null;\n  const cached = toolByCallId.get(callId) ?? { name: \"tool\", description: callId };\n  if (type === \"session.next.tool.input.started\") {\n    const name = stringValue4(data.name) ?? cached.name;\n    const description = name;\n    toolByCallId.set(callId, { name, description, inputText: \"\" });\n    return openCodeToolUpdate(callId, name, description, \"Pending\", \"\");\n  }\n  if (type === \"session.next.tool.input.delta\") {\n    const delta = stringValue4(data.delta) ?? \"\";\n    const nextInputText = (cached.inputText ?? \"\") + delta;\n    toolByCallId.set(callId, { ...cached, inputText: nextInputText });\n    return openCodeToolUpdate(callId, cached.name, cached.description, \"Pending\", nextInputText);\n  }\n  if (type === \"session.next.tool.input.ended\") {\n    const nextInputText = stringValue4(data.text) ?? cached.inputText ?? \"\";\n    const description = nextInputText || cached.description;\n    toolByCallId.set(callId, { ...cached, description, inputText: nextInputText });\n    return openCodeToolUpdate(callId, cached.name, description, \"Pending\", nextInputText);\n  }\n  if (type === \"session.next.tool.called\") {\n    const name = stringValue4(data.tool) ?? cached.name;\n    const description = openCodeToolTitle({ tool: name }, { input: data.input });\n    toolByCallId.set(callId, { name, description });\n    return openCodeToolUpdate(callId, name, description, \"Executing\", \"\");\n  }\n  if (type === \"session.next.tool.progress\") {\n    return openCodeToolUpdate(callId, cached.name, cached.description, \"Executing\", openCodeNextToolDisplay(data));\n  }\n  if (type === \"session.next.tool.success\") {\n    return openCodeToolUpdate(callId, cached.name, cached.description, \"Success\", openCodeNextToolDisplay(data));\n  }\n  if (type === \"session.next.tool.failed\") {\n    return openCodeToolUpdate(callId, cached.name, cached.description, \"Error\", openCodeNextToolErrorDisplay(data));\n  }\n  return null;\n}\nfunction openCodeToolUpdate(callId, name, description, status, resultDisplay) {\n  return {\n    callId,\n    call_id: callId,\n    name,\n    description,\n    status,\n    resultDisplay,\n    result_display: resultDisplay\n  };\n}\nfunction createOpenCodePermissionEventExtractor(sessionId) {\n  return (event) => {\n    const request = extractOpenCodePermissionAsked(event, sessionId);\n    if (request) return { type: \"asked\", request };\n    const requestId = extractOpenCodePermissionResolved(event, sessionId);\n    if (requestId) return { type: \"resolved\", requestId };\n    return null;\n  };\n}\nfunction extractOpenCodePermissionAsked(event, sessionId) {\n  if (!isRecord5(event)) return null;\n  const payload = isRecord5(event.payload) ? event.payload : event;\n  const type = typeof payload.type === \"string\" ? payload.type : \"\";\n  const data = isRecord5(payload.data) ? payload.data : isRecord5(payload.properties) ? payload.properties : {};\n  if (type !== \"permission.v2.asked\" && type !== \"permission.asked\") return null;\n  return data.sessionID === sessionId && typeof data.id === \"string\" ? data : null;\n}\nfunction extractOpenCodePermissionResolved(event, sessionId) {\n  if (!isRecord5(event)) return \"\";\n  const payload = isRecord5(event.payload) ? event.payload : event;\n  const type = typeof payload.type === \"string\" ? payload.type : \"\";\n  const data = isRecord5(payload.data) ? payload.data : isRecord5(payload.properties) ? payload.properties : {};\n  if (type !== \"permission.v2.replied\" && type !== \"permission.replied\") return \"\";\n  if (data.sessionID !== sessionId) return \"\";\n  return typeof data.requestID === \"string\" ? data.requestID : typeof data.id === \"string\" ? data.id : \"\";\n}\nfunction createOpenCodeQuestionEventExtractor(sessionId) {\n  return (event) => {\n    const request = extractOpenCodeQuestionAsked(event, sessionId);\n    if (request) return { type: \"asked\", request };\n    const requestId = extractOpenCodeQuestionResolved(event, sessionId);\n    if (requestId) return { type: \"resolved\", requestId };\n    return null;\n  };\n}\nfunction extractOpenCodeQuestionAsked(event, sessionId) {\n  if (!isRecord5(event)) return null;\n  const payload = isRecord5(event.payload) ? event.payload : event;\n  const type = typeof payload.type === \"string\" ? payload.type : \"\";\n  const data = isRecord5(payload.data) ? payload.data : isRecord5(payload.properties) ? payload.properties : {};\n  if (type !== \"question.v2.asked\" && type !== \"question.asked\") return null;\n  return data.sessionID === sessionId && typeof data.id === \"string\" ? data : null;\n}\nfunction extractOpenCodeQuestionResolved(event, sessionId) {\n  if (!isRecord5(event)) return \"\";\n  const payload = isRecord5(event.payload) ? event.payload : event;\n  const type = typeof payload.type === \"string\" ? payload.type : \"\";\n  const data = isRecord5(payload.data) ? payload.data : isRecord5(payload.properties) ? payload.properties : {};\n  if (type !== \"question.v2.replied\" && type !== \"question.replied\" && type !== \"question.v2.rejected\" && type !== \"question.rejected\") {\n    return \"\";\n  }\n  if (data.sessionID !== sessionId) return \"\";\n  return typeof data.requestID === \"string\" ? data.requestID : typeof data.id === \"string\" ? data.id : \"\";\n}\nfunction openCodeToolTitle(part, state) {\n  if (typeof state.title === \"string\" && state.title) return state.title;\n  if (isRecord5(state.input)) {\n    const values = Object.values(state.input).filter((value) => typeof value === \"string\" && value.trim()).slice(0, 2);\n    if (values.length) return values.join(\" \");\n  }\n  return typeof part.tool === \"string\" && part.tool ? part.tool : \"tool\";\n}\nfunction openCodeToolStatus(status) {\n  if (status === \"completed\") return \"Success\";\n  if (status === \"error\") return \"Error\";\n  if (status === \"canceled\" || status === \"cancelled\") return \"Canceled\";\n  if (status === \"pending\") return \"Pending\";\n  if (status === \"confirming\") return \"Confirming\";\n  return \"Executing\";\n}\nfunction openCodeToolResultDisplay(state) {\n  if (typeof state.output === \"string\" && state.output) return state.output;\n  if (typeof state.error === \"string\" && state.error) return state.error;\n  if (Array.isArray(state.content) && state.content.length) return JSON.stringify(state.content);\n  return \"\";\n}\nfunction openCodeNextToolDisplay(data) {\n  if (Array.isArray(data.content)) {\n    const content = data.content.map(openCodeToolContentDisplay).filter(Boolean).join(\"\\n\");\n    if (content) return content;\n  }\n  if (typeof data.result === \"string\" && data.result) return data.result;\n  if (data.result !== void 0) return jsonDisplay(data.result);\n  if (isRecord5(data.structured) && Object.keys(data.structured).length) return jsonDisplay(data.structured);\n  if (Array.isArray(data.outputPaths) && data.outputPaths.length) {\n    return data.outputPaths.filter((item) => typeof item === \"string\" && item.length > 0).join(\"\\n\");\n  }\n  return \"\";\n}\nfunction openCodeToolContentDisplay(item) {\n  if (typeof item === \"string\") return item;\n  if (!isRecord5(item)) return \"\";\n  if (typeof item.text === \"string\") return item.text;\n  if (typeof item.content === \"string\") return item.content;\n  if (typeof item.data === \"string\") return item.data;\n  if (typeof item.name === \"string\") return item.name;\n  return \"\";\n}\nfunction openCodeNextToolErrorDisplay(data) {\n  const error = data.error;\n  if (typeof error === \"string\") return error;\n  if (isRecord5(error) && typeof error.message === \"string\") return error.message;\n  if (error !== void 0) return jsonDisplay(error);\n  if (data.result !== void 0) return jsonDisplay(data.result);\n  return \"\";\n}\nfunction commandSendPath(sessionId, input) {\n  const params = new URLSearchParams();\n  if (input.directory) params.set(\"directory\", input.directory);\n  if (input.workspace) params.set(\"workspace\", input.workspace);\n  const query = params.toString();\n  return `/session/${encodeURIComponent(sessionId)}/command${query ? `?${query}` : \"\"}`;\n}\nfunction commandListV2Path(input) {\n  const location = {};\n  if (input.directory) location.directory = input.directory;\n  if (input.workspace) location.workspace = input.workspace;\n  if (Object.keys(location).length === 0) return \"/api/command\";\n  return `/api/command?location=${encodeURIComponent(JSON.stringify(location))}`;\n}\nfunction commandListLegacyPath(input) {\n  const params = new URLSearchParams();\n  if (input.directory) params.set(\"directory\", input.directory);\n  if (input.workspace) params.set(\"workspace\", input.workspace);\n  const query = params.toString();\n  return query ? `/command?${query}` : \"/command\";\n}\nfunction parseOpenCodeModelRef(model) {\n  if (typeof model !== \"string\" || !model.includes(\"/\")) return null;\n  const index = model.indexOf(\"/\");\n  const providerID = model.slice(0, index).trim();\n  const id = model.slice(index + 1).trim();\n  return providerID && id ? { providerID, id } : null;\n}\nfunction extractOpenCodeAssistantText(messages) {\n  for (const message of [...messages].reverse()) {\n    if (!isAssistantMessage(message)) continue;\n    const text = extractTextParts(message);\n    if (text) return text;\n  }\n  return \"\";\n}\nfunction extractOpenCodeCommands(value) {\n  const rawCommands = Array.isArray(value) ? value : isRecord5(value) && Array.isArray(value.data) ? value.data : [];\n  return rawCommands.filter(isRecord5).map((command) => {\n    const name = stringValue4(command.name) ?? stringValue4(command.command);\n    const template = stringValue4(command.template);\n    const description = stringValue4(command.description) ?? template;\n    if (!name || !description) return null;\n    return {\n      command: name,\n      description,\n      ...template ? { hint: template } : {}\n    };\n  }).filter((command) => command !== null);\n}\nfunction extractOpenCodeModels(value) {\n  const rawModels = Array.isArray(value) ? value : isRecord5(value) && Array.isArray(value.data) ? value.data : [];\n  return rawModels.filter(isRecord5).map((model) => {\n    if (model.enabled === false) return null;\n    const id = stringValue4(model.id);\n    const providerId = stringValue4(model.providerID) ?? stringValue4(model.providerId) ?? stringValue4(model.provider);\n    if (!id || !providerId) return null;\n    const name = stringValue4(model.name) ?? id;\n    return {\n      id: `${providerId}/${id}`,\n      label: `${name} (${providerId})`\n    };\n  }).filter((model) => model !== null);\n}\nfunction isAssistantMessage(value) {\n  if (!isRecord5(value)) return false;\n  return value.role === \"assistant\" || value.type === \"assistant\";\n}\nfunction extractTextParts(value) {\n  const candidates = [value.parts, value.content];\n  for (const candidate of candidates) {\n    if (!Array.isArray(candidate)) continue;\n    const text = candidate.map((part) => isRecord5(part) && part.type === \"text\" && typeof part.text === \"string\" ? part.text : \"\").join(\"\");\n    if (text) return text;\n  }\n  return typeof value.text === \"string\" ? value.text : \"\";\n}\nfunction isRecord5(value) {\n  return typeof value === \"object\" && value !== null && !Array.isArray(value);\n}\nfunction stringValue4(value) {\n  return typeof value === \"string\" && value.length > 0 ? value : void 0;\n}\nfunction numberValue(value) {\n  return typeof value === \"number\" && Number.isFinite(value) && value > 0 ? value : 0;\n}\nfunction jsonDisplay(value) {\n  try {\n    return JSON.stringify(value);\n  } catch {\n    return String(value);\n  }\n}\n\n// packages/daemon/src/agent-adapters/opencode-server.ts\nfunction parseOpenCodeServeUrl(line) {\n  const match = line.match(/server listening on\\s+(\\S+)/i);\n  if (!match) return null;\n  const value = match[1];\n  if (/^https?:\\/\\//i.test(value)) return value;\n  if (/^\\[.+\\]:\\d+$/.test(value)) return `http://${value}`;\n  if (/^[\\w.-]+:\\d+$/.test(value)) return `http://${value}`;\n  return null;\n}\nfunction createOpenCodeServerManager(options) {\n  const port2 = options?.port ?? 4096;\n  const startProcess = options?.startProcess ?? startLocalProcess;\n  const createClient = options?.createClient ?? ((baseUrl) => createOpenCodeClient({ baseUrl }));\n  const startupTimeoutMs = options?.startupTimeoutMs ?? 3e4;\n  let processRef = null;\n  let clientPromise = null;\n  return {\n    ensureClient() {\n      clientPromise ??= new Promise((resolve2, reject) => {\n        const spec = buildOpenCodeServeCommand({ port: port2 });\n        const proc = startProcess(spec.command, spec.args);\n        processRef = proc;\n        const timer = setTimeout(() => {\n          reject(new Error(`OpenCode server did not report a listening URL within ${startupTimeoutMs}ms`));\n        }, startupTimeoutMs);\n        proc.onStdout((chunk) => {\n          const url = parseOpenCodeServeUrl(chunk);\n          if (!url) return;\n          clearTimeout(timer);\n          resolve2(createClient(url));\n        });\n        proc.onStderr?.((chunk) => {\n          const url = parseOpenCodeServeUrl(chunk);\n          if (!url) return;\n          clearTimeout(timer);\n          resolve2(createClient(url));\n        });\n        proc.onExit?.((code, signal) => {\n          clearTimeout(timer);\n          clientPromise = null;\n          processRef = null;\n          reject(new Error(`OpenCode server exited before startup: code=${code ?? \"null\"} signal=${signal ?? \"null\"}`));\n        });\n      });\n      return clientPromise;\n    },\n    stop() {\n      processRef?.kill();\n      processRef = null;\n      clientPromise = null;\n    }\n  };\n}\nfunction startLocalProcess(command, args) {\n  const proc = spawn2(command, args, {\n    stdio: [\"ignore\", \"pipe\", \"pipe\"]\n  });\n  return {\n    onStdout(callback) {\n      proc.stdout?.on(\"data\", (chunk) => callback(chunk.toString(\"utf8\")));\n    },\n    onStderr(callback) {\n      proc.stderr?.on(\"data\", (chunk) => callback(chunk.toString(\"utf8\")));\n    },\n    onExit(callback) {\n      proc.on(\"exit\", callback);\n      proc.on(\"error\", (error) => {\n        proc.emit(\"exit\", 1, null);\n        console.error(\"[opencode] failed to start server:\", error);\n      });\n    },\n    kill() {\n      proc.kill();\n    }\n  };\n}\n\n// packages/daemon/src/agent-adapters/registry.ts\nfunction createAgentAdapterRegistry(adapters) {\n  const byBackend = new Map(adapters.map((adapter) => [adapter.backend, adapter]));\n  return {\n    listAgents() {\n      return adapters.map(({ backend, name, label }) => ({ backend, name, label }));\n    },\n    get(backend) {\n      return byBackend.get(backend);\n    },\n    probeAll() {\n      return Promise.all(adapters.map((adapter) => adapter.probe()));\n    }\n  };\n}\n\n// packages/daemon/src/agent-adapters/default-registry.ts\nfunction createDefaultAgentAdapterRegistry() {\n  return createAgentAdapterRegistry([\n    createOpenCodeAdapter(localCommandRunner, { serverManager: createOpenCodeServerManager() }),\n    createGeminiAdapter(localCommandRunner),\n    createCodexAdapter(localCommandRunner)\n  ]);\n}\n\n// packages/daemon/src/bridge-router.ts\nvar BridgeRouter = class {\n  handlers = /* @__PURE__ */ new Map();\n  register(key, handler) {\n    this.handlers.set(key, handler);\n  }\n  async handleIncoming(message, options) {\n    const request = parseBridgeRequestMessage(message);\n    if (!request) {\n      return [];\n    }\n    const handler = this.handlers.get(request.key);\n    if (!handler) {\n      return [\n        createBridgeCallbackMessage(\n          request.key,\n          request.id,\n          bridgeError(\"BRIDGE_ROUTE_NOT_FOUND\", `No bridge handler registered for '${request.key}'`)\n        )\n      ];\n    }\n    const pushedMessages = [];\n    const detachedWork = [];\n    const liveEmit = options?.emit;\n    const context = {\n      key: request.key,\n      id: request.id,\n      emit(name, data) {\n        const pushed = { name, data };\n        if (liveEmit) {\n          liveEmit(pushed);\n        } else {\n          pushedMessages.push(pushed);\n        }\n      },\n      runDetached(work) {\n        const task = Promise.resolve().then(work);\n        if (liveEmit) {\n          void task.catch((error) => {\n            console.warn(\n              \"[BridgeRouter] detached route work failed:\",\n              error instanceof Error ? error.message : error\n            );\n          });\n        } else {\n          detachedWork.push(task);\n        }\n      }\n    };\n    try {\n      const data = await handler(request.data, context);\n      if (!liveEmit && detachedWork.length > 0) {\n        await Promise.all(detachedWork);\n      }\n      return [createBridgeCallbackMessage(request.key, request.id, data), ...pushedMessages];\n    } catch (error) {\n      const message2 = error instanceof Error ? error.message : \"Bridge route failed\";\n      return [\n        createBridgeCallbackMessage(\n          request.key,\n          request.id,\n          bridgeError(\"BRIDGE_ROUTE_FAILED\", message2)\n        )\n      ];\n    }\n  }\n};\nfunction bridgeError(code, message) {\n  return {\n    success: false,\n    error: {\n      code,\n      message\n    }\n  };\n}\n\n// packages/daemon/src/conversation-store.ts\nimport { mkdirSync, readFileSync, renameSync, rmSync, writeFileSync } from \"node:fs\";\nimport { dirname } from \"node:path\";\nvar InMemoryConversationStore = class {\n  conversations = /* @__PURE__ */ new Map();\n  messages = /* @__PURE__ */ new Map();\n  artifacts = /* @__PURE__ */ new Map();\n  now;\n  id;\n  constructor(options) {\n    this.now = options?.now ?? Date.now;\n    this.id = options?.id ?? randomId;\n    if (options?.initialSnapshot) {\n      this.restoreSnapshot(options.initialSnapshot);\n    }\n  }\n  createConversation(input) {\n    const timestamp = this.now();\n    const conversation = {\n      id: this.id(),\n      name: input.name?.trim() || \"New conversation\",\n      type: input.type || \"acp\",\n      status: \"finished\",\n      runtime: {\n        state: \"idle\",\n        can_send_message: true,\n        has_task: false,\n        task_status: \"finished\",\n        is_processing: false,\n        pending_confirmations: 0,\n        turn_id: null\n      },\n      createTime: timestamp,\n      modifyTime: timestamp,\n      model: input.model || { id: \"\", useModel: \"\" },\n      extra: input.extra || {}\n    };\n    this.conversations.set(conversation.id, conversation);\n    this.messages.set(conversation.id, []);\n    this.artifacts.set(conversation.id, []);\n    this.didMutate();\n    return conversation;\n  }\n  listConversations(page = 0, pageSize = 100) {\n    const start = Math.max(0, page) * Math.max(1, pageSize);\n    const end = start + Math.max(1, pageSize);\n    return [...this.conversations.values()].sort((a, b) => b.modifyTime - a.modifyTime).slice(start, end);\n  }\n  getMessages(conversationId) {\n    return [...this.messages.get(conversationId) ?? []];\n  }\n  getConversation(conversationId) {\n    return this.conversations.get(conversationId);\n  }\n  listArtifacts(conversationId) {\n    this.requireConversation(conversationId);\n    return [...this.artifacts.get(conversationId) ?? []].sort((a, b) => a.created_at - b.created_at);\n  }\n  upsertArtifact(artifact) {\n    this.requireConversation(artifact.conversation_id);\n    const current = this.artifacts.get(artifact.conversation_id) ?? [];\n    const index = current.findIndex((candidate) => candidate.id === artifact.id);\n    const next = [...current];\n    if (index === -1) {\n      next.push(artifact);\n    } else {\n      next[index] = artifact;\n    }\n    this.artifacts.set(artifact.conversation_id, next);\n    this.didMutate();\n    return artifact;\n  }\n  updateArtifactStatus(conversationId, artifactId, status) {\n    this.requireConversation(conversationId);\n    const current = this.artifacts.get(conversationId) ?? [];\n    const index = current.findIndex((artifact) => artifact.id === artifactId);\n    if (index === -1) return null;\n    const updated = {\n      ...current[index],\n      status,\n      updated_at: this.now()\n    };\n    const next = [...current];\n    next[index] = updated;\n    this.artifacts.set(conversationId, next);\n    this.didMutate();\n    return updated;\n  }\n  addTextMessage(input) {\n    const conversation = this.requireConversation(input.conversationId);\n    const timestamp = this.now();\n    const message = {\n      id: this.id(),\n      msg_id: input.msgId || this.id(),\n      conversation_id: input.conversationId,\n      type: \"text\",\n      position: input.position,\n      content: { content: input.content },\n      createdAt: timestamp\n    };\n    this.messages.get(input.conversationId)?.push(message);\n    conversation.modifyTime = timestamp;\n    this.didMutate();\n    return message;\n  }\n  upsertToolGroupMessage(input) {\n    const tool = isRecord6(input.tool) ? input.tool : null;\n    const callId = tool && typeof tool.callId === \"string\" ? tool.callId : null;\n    if (!tool || !callId) return null;\n    const conversation = this.requireConversation(input.conversationId);\n    const list = this.messages.get(input.conversationId) ?? [];\n    const timestamp = this.now();\n    for (const message2 of list) {\n      if (message2.type !== \"tool_group\" || !Array.isArray(message2.content)) continue;\n      const toolIndex = message2.content.findIndex(\n        (candidate) => isRecord6(candidate) && candidate.callId === callId\n      );\n      if (toolIndex === -1) continue;\n      const current = message2.content[toolIndex];\n      message2.content[toolIndex] = isRecord6(current) ? { ...current, ...tool } : tool;\n      conversation.modifyTime = timestamp;\n      this.didMutate();\n      return message2;\n    }\n    const message = {\n      id: this.id(),\n      msg_id: input.msgId,\n      conversation_id: input.conversationId,\n      type: \"tool_group\",\n      content: [tool],\n      createdAt: timestamp\n    };\n    list.push(message);\n    this.messages.set(input.conversationId, list);\n    conversation.modifyTime = timestamp;\n    this.didMutate();\n    return message;\n  }\n  upsertStructuredMessage(input) {\n    const content = isRecord6(input.content) ? input.content : null;\n    const identity = content ? firstStringProperty(content, input.identityKeys) : null;\n    if (!content || !identity) return null;\n    const conversation = this.requireConversation(input.conversationId);\n    const list = this.messages.get(input.conversationId) ?? [];\n    const timestamp = this.now();\n    for (const message2 of list) {\n      if (message2.type !== input.type || !isRecord6(message2.content)) continue;\n      if (firstStringProperty(message2.content, input.identityKeys) !== identity) continue;\n      message2.content = { ...message2.content, ...content };\n      conversation.modifyTime = timestamp;\n      this.didMutate();\n      return message2;\n    }\n    const message = {\n      id: this.id(),\n      msg_id: input.msgId,\n      conversation_id: input.conversationId,\n      type: input.type,\n      position: \"left\",\n      content,\n      createdAt: timestamp\n    };\n    list.push(message);\n    this.messages.set(input.conversationId, list);\n    conversation.modifyTime = timestamp;\n    this.didMutate();\n    return message;\n  }\n  removeConversation(id) {\n    const existed = this.conversations.delete(id);\n    this.messages.delete(id);\n    this.artifacts.delete(id);\n    if (existed) this.didMutate();\n    return existed;\n  }\n  updateConversation(id, updates) {\n    const conversation = this.conversations.get(id);\n    if (!conversation) return false;\n    this.conversations.set(id, {\n      ...conversation,\n      ...updates,\n      extra: updates.extra ? { ...conversation.extra, ...updates.extra } : conversation.extra,\n      modifyTime: this.now()\n    });\n    this.didMutate();\n    return true;\n  }\n  exportSnapshot() {\n    return {\n      version: 1,\n      conversations: [...this.conversations.values()],\n      messages: Object.fromEntries(this.messages.entries()),\n      artifacts: Object.fromEntries(this.artifacts.entries())\n    };\n  }\n  didMutate() {\n  }\n  restoreSnapshot(snapshot) {\n    for (const rawConversation of snapshot.conversations) {\n      if (!isConversation(rawConversation)) continue;\n      const conversation = normalizeRestoredConversation(rawConversation);\n      this.conversations.set(conversation.id, conversation);\n      this.messages.set(conversation.id, validMessages(snapshot.messages[conversation.id], conversation.id));\n      this.artifacts.set(conversation.id, validArtifacts(snapshot.artifacts[conversation.id], conversation.id));\n    }\n  }\n  requireConversation(id) {\n    const conversation = this.conversations.get(id);\n    if (!conversation) {\n      throw new Error(`Conversation '${id}' was not found`);\n    }\n    return conversation;\n  }\n};\nvar JsonConversationStore = class extends InMemoryConversationStore {\n  filePath;\n  constructor(filePath, options) {\n    super({ ...options, initialSnapshot: readSnapshot(filePath) });\n    this.filePath = filePath;\n  }\n  didMutate() {\n    const directory = dirname(this.filePath);\n    const temporaryPath = `${this.filePath}.${process.pid}.tmp`;\n    mkdirSync(directory, { recursive: true, mode: 448 });\n    try {\n      writeFileSync(temporaryPath, `${JSON.stringify(this.exportSnapshot(), null, 2)}\n`, {\n        encoding: \"utf8\",\n        mode: 384\n      });\n      renameSync(temporaryPath, this.filePath);\n    } catch (error) {\n      rmSync(temporaryPath, { force: true });\n      throw error;\n    }\n  }\n};\nfunction readSnapshot(filePath) {\n  try {\n    const value = JSON.parse(readFileSync(filePath, \"utf8\"));\n    return isConversationStoreSnapshot(value) ? value : void 0;\n  } catch {\n    return void 0;\n  }\n}\nfunction isConversationStoreSnapshot(value) {\n  if (!isRecord6(value) || value.version !== 1) return false;\n  return Array.isArray(value.conversations) && isRecord6(value.messages) && isRecord6(value.artifacts);\n}\nfunction isConversation(value) {\n  return isRecord6(value) && typeof value.id === \"string\" && typeof value.name === \"string\" && typeof value.createTime === \"number\" && typeof value.modifyTime === \"number\" && isRecord6(value.model) && isRecord6(value.extra);\n}\nfunction validMessages(value, conversationId) {\n  if (!Array.isArray(value)) return [];\n  return value.filter((message) => isStoredMessage(message, conversationId));\n}\nfunction isStoredMessage(value, conversationId) {\n  if (!isRecord6(value) || value.conversation_id !== conversationId || typeof value.id !== \"string\" || typeof value.msg_id !== \"string\" || typeof value.type !== \"string\" || typeof value.createdAt !== \"number\") {\n    return false;\n  }\n  if (value.type === \"text\") {\n    return (value.position === \"left\" || value.position === \"right\") && isRecord6(value.content) && typeof value.content.content === \"string\";\n  }\n  if (value.type === \"tool_group\") return Array.isArray(value.content);\n  if (value.type === \"tool_call\" || value.type === \"acp_tool_call\" || value.type === \"codex_tool_call\" || value.type === \"plan\") {\n    return isRecord6(value.content);\n  }\n  return false;\n}\nfunction validArtifacts(value, conversationId) {\n  if (!Array.isArray(value)) return [];\n  return value.filter(\n    (artifact) => isRecord6(artifact) && artifact.conversation_id === conversationId && typeof artifact.id === \"string\" && (artifact.kind === \"cron_trigger\" || artifact.kind === \"skill_suggest\") && (artifact.status === \"active\" || artifact.status === \"pending\" || artifact.status === \"dismissed\" || artifact.status === \"saved\") && isRecord6(artifact.payload) && typeof artifact.created_at === \"number\" && typeof artifact.updated_at === \"number\"\n  );\n}\nfunction normalizeRestoredConversation(conversation) {\n  const runtime = conversation.runtime;\n  const wasInterrupted = conversation.status === \"running\" || conversation.status === \"waiting_confirmation\" || Boolean(runtime && (runtime.state !== \"idle\" || runtime.is_processing || runtime.has_task));\n  if (!wasInterrupted) return conversation;\n  return {\n    ...conversation,\n    status: \"finished\",\n    runtime: {\n      state: \"idle\",\n      can_send_message: true,\n      has_task: false,\n      task_status: \"finished\",\n      is_processing: false,\n      pending_confirmations: 0,\n      turn_id: null\n    }\n  };\n}\nfunction isRecord6(value) {\n  return typeof value === \"object\" && value !== null;\n}\nfunction firstStringProperty(value, keys) {\n  for (const key of keys) {\n    if (typeof value[key] === \"string\" && value[key]) return value[key];\n  }\n  return null;\n}\nfunction randomId() {\n  return `local_${Date.now().toString(36)}_${Math.random().toString(36).slice(2, 10)}`;\n}\n\n// packages/daemon/src/local-files.ts\nimport { execFile as execFile2 } from \"node:child_process\";\nimport { lstat, readFile, readdir, rename, rm } from \"node:fs/promises\";\nimport { basename as basename2, dirname as dirname2, join, relative as relative4, resolve } from \"node:path\";\nimport { promisify as promisify2 } from \"node:util\";\nvar IGNORED_ENTRY_NAMES = /* @__PURE__ */ new Set([\".git\", \"node_modules\", \".expo\", \".next\", \"dist\", \"build\"]);\nvar MAX_WORKSPACE_TREE_DEPTH = 4;\nvar MAX_WORKSPACE_TREE_ENTRIES = 1e3;\nvar MAX_TEXT_FILE_BYTES = 1024 * 1024 * 2;\nvar MAX_IMAGE_FILE_BYTES = 1024 * 1024 * 10;\nvar execFileAsync2 = promisify2(execFile2);\nasync function getWorkspaceTree(params) {\n  const workspace = resolveLocalPath(requiredString(params.workspace ?? params.path));\n  const targetPath = resolveLocalPath(typeof params.path === \"string\" ? params.path : workspace);\n  const search = typeof params.search === \"string\" ? params.search.trim() : \"\";\n  const target = ensurePathInsideRoot(targetPath, workspace);\n  const rootNode = await readDirectoryNode(target, workspace, 0, search, { count: 0 });\n  return rootNode ? [rootNode] : [];\n}\nasync function getFileTreeByDir(params) {\n  const root = resolveLocalPath(requiredString(params.root ?? params.dir));\n  const dir = ensurePathInsideRoot(resolveLocalPath(requiredString(params.dir)), root);\n  const rootNode = await readDirectoryNode(dir, root, 0, \"\", { count: 0 });\n  return rootNode ? [rootNode] : [];\n}\nasync function readTextFile(path) {\n  const filePath = resolveLocalPath(path);\n  const fileStats = await lstat(filePath);\n  if (!fileStats.isFile()) throw new Error(`Path is not a file: ${filePath}`);\n  if (fileStats.size > MAX_TEXT_FILE_BYTES) {\n    throw new Error(`File is too large to preview: ${filePath}`);\n  }\n  return await readFile(filePath, \"utf8\");\n}\nasync function readImageBase64(path) {\n  const filePath = resolveLocalPath(path);\n  const fileStats = await lstat(filePath);\n  if (!fileStats.isFile()) throw new Error(`Path is not a file: ${filePath}`);\n  if (fileStats.size > MAX_IMAGE_FILE_BYTES) {\n    throw new Error(`Image file is too large to preview: ${filePath}`);\n  }\n  const buffer = await readFile(filePath);\n  return `data:${imageMimeType(filePath)};base64,${buffer.toString(\"base64\")}`;\n}\nasync function removeWorkspaceEntry(params) {\n  const workspace = resolveLocalPath(requiredString(params.workspace));\n  const rawPath = typeof params.path === \"string\" ? params.path : typeof params.file_path === \"string\" ? params.file_path : join(workspace, requiredString(params.relativePath));\n  const targetPath = ensurePathInsideRoot(resolveLocalPath(rawPath), workspace);\n  if (targetPath === workspace) {\n    throw new Error(\"Refusing to remove the workspace root\");\n  }\n  await rm(targetPath, { recursive: true, force: true });\n  return { success: true };\n}\nasync function renameWorkspaceEntry(params) {\n  const workspace = resolveLocalPath(requiredString(params.workspace));\n  const rawPath = typeof params.path === \"string\" ? params.path : typeof params.file_path === \"string\" ? params.file_path : join(workspace, requiredString(params.relativePath));\n  const targetPath = ensurePathInsideRoot(resolveLocalPath(rawPath), workspace);\n  if (targetPath === workspace) {\n    throw new Error(\"Refusing to rename the workspace root\");\n  }\n  const newName = requiredString(params.new_name ?? params.newName).trim();\n  if (!newName) throw new Error(\"Expected non-empty new name\");\n  if (newName === \".\" || newName === \"..\" || newName.includes(\"/\") || newName.includes(\"\\\\\") || newName.includes(\"\\0\")) {\n    throw new Error(\"New name must be a single path segment\");\n  }\n  const newPath = ensurePathInsideRoot(resolve(dirname2(targetPath), newName), workspace);\n  await rename(targetPath, newPath);\n  return { new_path: newPath };\n}\nasync function compareWorkspaceChanges(params) {\n  const workspace = resolveLocalPath(requiredString(params.workspace));\n  const root = ensurePathInsideRoot(workspace, workspace);\n  if (!await isGitWorkspace(root)) {\n    return { mode: \"snapshot\", branch: null, staged: [], unstaged: [] };\n  }\n  const branch = await readGitBranch(root);\n  const status = await readGitStatus(root);\n  const stagedStats = await readGitNumstat(root, [\"diff\", \"--cached\", \"--numstat\"]);\n  const unstagedStats = await readGitNumstat(root, [\"diff\", \"--numstat\"]);\n  const staged = /* @__PURE__ */ new Map();\n  const unstaged = /* @__PURE__ */ new Map();\n  for (const item of status) {\n    if (item.indexStatus && item.indexStatus !== \"?\") {\n      staged.set(\n        item.path,\n        buildWorkspaceFileChange(root, item.path, operationFromGitStatus(item.indexStatus), stagedStats.get(item.path))\n      );\n    }\n    if (item.worktreeStatus) {\n      const stats = item.worktreeStatus === \"?\" ? await readUntrackedStats(root, item.path) : unstagedStats.get(item.path);\n      unstaged.set(\n        item.path,\n        buildWorkspaceFileChange(root, item.path, operationFromGitStatus(item.worktreeStatus), stats)\n      );\n    }\n  }\n  return {\n    mode: \"git-repo\",\n    branch,\n    staged: [...staged.values()].sort(compareFileChanges),\n    unstaged: [...unstaged.values()].sort(compareFileChanges)\n  };\n}\nasync function readWorkspaceFileDiff(params) {\n  const { workspace, relativePath } = workspaceChangePathParams(params);\n  const source = params.source === \"staged\" ? \"staged\" : \"unstaged\";\n  const filePath = ensurePathInsideRoot(resolve(join(workspace, relativePath)), workspace);\n  if (!await isGitWorkspace(workspace)) {\n    return { relativePath, source, diff: \"\" };\n  }\n  const status = await readGitStatus(workspace);\n  const statusItem = status.find((item) => item.path === relativePath);\n  const diff = source === \"staged\" ? await readGitDiff(workspace, [\"diff\", \"--cached\", \"--\", relativePath]) : statusItem?.worktreeStatus === \"?\" ? await readGitDiff(workspace, [\"diff\", \"--no-index\", \"--\", \"/dev/null\", filePath], true) : await readGitDiff(workspace, [\"diff\", \"--\", relativePath]);\n  return { relativePath, source, diff };\n}\nasync function stageWorkspaceFile(params) {\n  const { workspace, relativePath } = workspaceChangePathParams(params);\n  if (!await isGitWorkspace(workspace)) return;\n  await runGit(workspace, [\"add\", \"--\", relativePath]);\n}\nasync function stageWorkspace(params) {\n  const workspace = resolveLocalPath(requiredString(params.workspace));\n  if (!await isGitWorkspace(workspace)) return;\n  await runGit(workspace, [\"add\", \"--all\", \"--\", \".\"]);\n}\nasync function unstageWorkspaceFile(params) {\n  const { workspace, relativePath } = workspaceChangePathParams(params);\n  if (!await isGitWorkspace(workspace)) return;\n  await runGit(workspace, [\"restore\", \"--staged\", \"--\", relativePath]);\n}\nasync function unstageWorkspace(params) {\n  const workspace = resolveLocalPath(requiredString(params.workspace));\n  if (!await isGitWorkspace(workspace)) return;\n  await runGit(workspace, [\"restore\", \"--staged\", \"--\", \".\"]);\n}\nasync function discardWorkspaceFile(params) {\n  const { workspace, relativePath } = workspaceChangePathParams(params);\n  const operation = params.operation === \"create\" ? \"create\" : params.operation === \"delete\" ? \"delete\" : \"modify\";\n  if (!await isGitWorkspace(workspace)) return;\n  if (operation === \"create\") {\n    await rm(ensurePathInsideRoot(resolve(join(workspace, relativePath)), workspace), { recursive: true, force: true });\n    return;\n  }\n  await runGit(workspace, [\"restore\", \"--\", relativePath]);\n}\nasync function readDirectoryNode(targetPath, rootPath, depth, search, counter) {\n  if (counter.count >= MAX_WORKSPACE_TREE_ENTRIES) return null;\n  let fileStats;\n  try {\n    fileStats = await lstat(targetPath);\n  } catch {\n    return null;\n  }\n  if (!fileStats.isDirectory() && !fileStats.isFile()) return null;\n  counter.count += 1;\n  const node = {\n    name: basename2(targetPath) || targetPath,\n    fullPath: targetPath,\n    relativePath: normalizeRelativePath4(relative4(rootPath, targetPath)),\n    isDir: fileStats.isDirectory(),\n    isFile: fileStats.isFile()\n  };\n  if (fileStats.isFile()) {\n    return matchesSearch(node, search) ? node : search ? null : node;\n  }\n  const children = [];\n  if (depth < MAX_WORKSPACE_TREE_DEPTH) {\n    let entries = [];\n    try {\n      entries = await readdir(targetPath, { withFileTypes: true });\n    } catch {\n      entries = [];\n    }\n    entries.sort((a, b) => {\n      if (a.isDirectory() !== b.isDirectory()) return a.isDirectory() ? -1 : 1;\n      return a.name.localeCompare(b.name);\n    });\n    for (const entry of entries) {\n      if (IGNORED_ENTRY_NAMES.has(entry.name)) continue;\n      if (counter.count >= MAX_WORKSPACE_TREE_ENTRIES) break;\n      const child = await readDirectoryNode(join(targetPath, entry.name), rootPath, depth + 1, search, counter);\n      if (child) children.push(child);\n    }\n  }\n  if (children.length > 0) node.children = children;\n  if (!search || matchesSearch(node, search) || children.length > 0) return node;\n  return null;\n}\nfunction matchesSearch(node, search) {\n  if (!search) return true;\n  const lower = search.toLowerCase();\n  return node.name.toLowerCase().includes(lower) || node.relativePath.toLowerCase().includes(lower);\n}\nfunction normalizeRelativePath4(path) {\n  return path === \"\" ? \"\" : path.split(\"\\\\\").join(\"/\");\n}\nfunction workspaceChangePathParams(params) {\n  const workspace = resolveLocalPath(requiredString(params.workspace));\n  const relativePath = normalizeRelativePath4(\n    typeof params.relativePath === \"string\" ? params.relativePath : relative4(workspace, resolveLocalPath(requiredString(params.file_path)))\n  );\n  ensurePathInsideRoot(resolve(join(workspace, relativePath)), workspace);\n  return { workspace, relativePath };\n}\nfunction resolveLocalPath(path) {\n  if (!path.trim()) throw new Error(\"Expected non-empty path\");\n  if (path.includes(\"\\0\")) throw new Error(\"Path contains a null byte\");\n  return resolve(path.replace(/^~(?=\\/|$)/, process.env.HOME || \".\"));\n}\nfunction ensurePathInsideRoot(path, root) {\n  const rel = relative4(root, path);\n  if (rel === \"\" || !rel.startsWith(\"..\") && !rel.startsWith(\"/\")) return path;\n  throw new Error(`Path is outside the workspace: ${path}`);\n}\nfunction imageMimeType(path) {\n  const ext = path.toLowerCase().split(\".\").pop() ?? \"\";\n  if (ext === \"jpg\" || ext === \"jpeg\") return \"image/jpeg\";\n  if (ext === \"gif\") return \"image/gif\";\n  if (ext === \"webp\") return \"image/webp\";\n  if (ext === \"svg\") return \"image/svg+xml\";\n  if (ext === \"bmp\") return \"image/bmp\";\n  if (ext === \"avif\") return \"image/avif\";\n  return \"image/png\";\n}\nasync function isGitWorkspace(workspace) {\n  try {\n    const { stdout } = await runGit(workspace, [\"rev-parse\", \"--is-inside-work-tree\"]);\n    return stdout.trim() === \"true\";\n  } catch {\n    return false;\n  }\n}\nasync function readGitBranch(workspace) {\n  try {\n    const { stdout } = await runGit(workspace, [\"rev-parse\", \"--abbrev-ref\", \"HEAD\"]);\n    const branch = stdout.trim();\n    return branch && branch !== \"HEAD\" ? branch : null;\n  } catch {\n    return null;\n  }\n}\nasync function readGitStatus(workspace) {\n  const { stdout } = await runGit(workspace, [\"status\", \"--porcelain=v1\"]);\n  return stdout.split(/\\r?\\n/).map((line) => parseGitStatusLine(line)).filter((item) => Boolean(item));\n}\nfunction parseGitStatusLine(line) {\n  if (line.length < 4) return null;\n  const indexStatus = line[0] === \" \" ? \"\" : line[0];\n  const worktreeStatus = line[1] === \" \" ? \"\" : line[1];\n  const rawPath = line.slice(3);\n  const path = normalizeGitStatusPath(rawPath);\n  if (!path) return null;\n  if (indexStatus === \"?\" && worktreeStatus === \"?\") {\n    return { indexStatus: \"\", worktreeStatus: \"?\", path };\n  }\n  return { indexStatus, worktreeStatus, path };\n}\nfunction normalizeGitStatusPath(path) {\n  const renamedPath = path.includes(\" -> \") ? path.split(\" -> \").pop() ?? path : path;\n  return normalizeRelativePath4(renamedPath.replace(/^\"|\"$/g, \"\"));\n}\nasync function readGitNumstat(workspace, args) {\n  const { stdout } = await runGit(workspace, args);\n  const result = /* @__PURE__ */ new Map();\n  for (const line of stdout.split(/\\r?\\n/)) {\n    if (!line.trim()) continue;\n    const [additions, deletions, ...pathParts] = line.split(\"\t\");\n    const path = normalizeRelativePath4(pathParts.join(\"\t\"));\n    if (!path) continue;\n    result.set(path, {\n      additions: parseGitStatNumber(additions),\n      deletions: parseGitStatNumber(deletions)\n    });\n  }\n  return result;\n}\nfunction parseGitStatNumber(value) {\n  const parsed = Number(value);\n  return Number.isFinite(parsed) && parsed > 0 ? parsed : 0;\n}\nasync function readUntrackedStats(workspace, relativePath) {\n  const filePath = ensurePathInsideRoot(resolve(join(workspace, relativePath)), workspace);\n  try {\n    const stats = await lstat(filePath);\n    if (!stats.isFile() || stats.size > MAX_TEXT_FILE_BYTES) return { additions: 0, deletions: 0 };\n    const content = await readFile(filePath, \"utf8\");\n    return { additions: countTextLines(content), deletions: 0 };\n  } catch {\n    return { additions: 0, deletions: 0 };\n  }\n}\nfunction countTextLines(content) {\n  if (!content) return 0;\n  const lines = content.split(/\\r\\n|\\r|\\n/);\n  return content.endsWith(\"\\n\") || content.endsWith(\"\\r\") ? lines.length - 1 : lines.length;\n}\nfunction operationFromGitStatus(status) {\n  if (status === \"A\" || status === \"?\" || status === \"R\" || status === \"C\") return \"create\";\n  if (status === \"D\") return \"delete\";\n  return \"modify\";\n}\nfunction buildWorkspaceFileChange(workspace, relativePath, operation, stats = { additions: 0, deletions: 0 }) {\n  const normalizedPath = normalizeRelativePath4(relativePath);\n  return {\n    file_path: ensurePathInsideRoot(resolve(join(workspace, normalizedPath)), workspace),\n    relativePath: normalizedPath,\n    operation,\n    additions: stats.additions,\n    deletions: stats.deletions\n  };\n}\nfunction compareFileChanges(left, right) {\n  return left.relativePath.localeCompare(right.relativePath);\n}\nasync function runGit(workspace, args) {\n  const result = await execFileAsync2(\"git\", args, {\n    cwd: workspace,\n    encoding: \"utf8\",\n    maxBuffer: 1024 * 1024 * 4\n  });\n  return {\n    stdout: String(result.stdout),\n    stderr: String(result.stderr)\n  };\n}\nasync function readGitDiff(workspace, args, allowExitOne = false) {\n  try {\n    const { stdout } = await runGit(workspace, args);\n    return stdout;\n  } catch (error) {\n    if (allowExitOne && isRecord7(error) && error.code === 1 && typeof error.stdout === \"string\") {\n      return error.stdout;\n    }\n    throw error;\n  }\n}\nfunction isRecord7(value) {\n  return typeof value === \"object\" && value !== null && !Array.isArray(value);\n}\nfunction requiredString(value) {\n  if (typeof value !== \"string\") throw new Error(\"Expected string bridge parameter\");\n  return value;\n}\n\n// packages/daemon/src/default-router.ts\nvar startedAt = Date.now();\nfunction createDefaultRouter(options) {\n  const router2 = new BridgeRouter();\n  const store = options?.store ?? new InMemoryConversationStore();\n  const adapters = options?.adapters ?? createDefaultAgentAdapterRegistry();\n  const pendingConfirmations = /* @__PURE__ */ new Map();\n  const activeRuns = /* @__PURE__ */ new Map();\n  const activeRunBackends = /* @__PURE__ */ new Map();\n  const activeTurnIds = /* @__PURE__ */ new Map();\n  router2.register(\"runtime.get-status\", async () => getRuntimeStatus(adapters));\n  router2.register(\"acp.get-available-agents\", () => ({\n    success: true,\n    data: adapters.listAgents()\n  }));\n  router2.register(\"acp.probe-model-info\", async (data) => ({\n    success: true,\n    data: { modelInfo: await getAdapterModelInfo(adapters, stringParam(asRecord(data).backend, \"\")) }\n  }));\n  router2.register(\"conversation.ensure-runtime\", async (data) => {\n    const conversation = requireConversation(store, stringParam(asRecord(data).conversation_id));\n    const configOptions = await buildConversationConfigOptions(adapters, conversation);\n    return {\n      recovered: false,\n      runtime: conversation.runtime ?? idleRuntimeSummary(\"finished\", pendingConfirmationCount(pendingConfirmations, conversation.id)),\n      config_options: configOptions,\n      modelInfo: modelInfoFromConfigOptions(configOptions)\n    };\n  });\n  router2.register(\"conversation.set-config-option\", async (data, context) => {\n    const params = asRecord(data);\n    const conversationId = stringParam(params.conversation_id);\n    const optionId = stringParam(params.option_id);\n    const value = stringParam(params.value);\n    const conversation = requireConversation(store, conversationId);\n    const currentConfigOptions = await buildConversationConfigOptions(adapters, conversation);\n    const option = currentConfigOptions.find((candidate) => candidate.id === optionId);\n    if (!option) {\n      throw new Error(`Config option '${optionId}' was not found`);\n    }\n    const selected = option.options.find((candidate) => candidate.value === value);\n    if (option.options.length > 0 && !selected) {\n      throw new Error(`Config option '${optionId}' does not support value '${value}'`);\n    }\n    const updates = getConfigOptionConversationUpdates(option, value, selected);\n    store.updateConversation(conversationId, updates);\n    emitConversationListChanged(context, conversationId, \"updated\");\n    const updatedConversation = requireConversation(store, conversationId);\n    const configOptions = await buildConversationConfigOptions(adapters, updatedConversation);\n    return {\n      confirmation: \"observed\",\n      config_options: configOptions,\n      modelInfo: modelInfoFromConfigOptions(configOptions)\n    };\n  });\n  router2.register(\"database.get-user-conversations\", (data) => {\n    const params = asRecord(data);\n    return store.listConversations(numberParam(params.page, 0), numberParam(params.pageSize, 100));\n  });\n  router2.register(\"database.get-conversation-messages\", (data) => {\n    const params = asRecord(data);\n    return store.getMessages(stringParam(params.conversation_id));\n  });\n  router2.register(\"conversation.get\", (data) => {\n    const params = asRecord(data);\n    return store.getConversation(stringParam(params.conversation_id)) ?? null;\n  });\n  router2.register(\"conversation.list-artifacts\", (data) => {\n    const params = asRecord(data);\n    return store.listArtifacts(stringParam(params.conversation_id));\n  });\n  router2.register(\"conversation.update-artifact\", (data, context) => {\n    const params = asRecord(data);\n    const conversationId = stringParam(params.conversation_id);\n    const artifactId = stringParam(params.artifact_id);\n    const status = artifactStatusParam(params.status);\n    const artifact = store.updateArtifactStatus(conversationId, artifactId, status);\n    if (!artifact) {\n      throw new Error(`Artifact '${artifactId}' was not found`);\n    }\n    context.emit(\"conversation.artifact\", artifact);\n    return artifact;\n  });\n  router2.register(\"conversation.get-slash-commands\", async (data) => {\n    const params = asRecord(data);\n    const conversationId = stringParam(params.conversation_id);\n    const conversation = store.getConversation(conversationId);\n    if (!conversation) return [];\n    const backend = conversation.extra.backend || \"opencode\";\n    const adapter = adapters.get(backend);\n    if (!adapter?.getSlashCommands) return [];\n    return adapter.getSlashCommands({\n      conversationId,\n      workspace: conversation.extra.workspace\n    });\n  });\n  router2.register(\"create-conversation\", (data, context) => {\n    const params = asRecord(data);\n    const extra = isRecord8(params.extra) ? params.extra : {};\n    const conversation = store.createConversation({\n      type: stringParam(params.type, \"acp\"),\n      name: stringParam(params.name, \"New conversation\"),\n      model: normalizeConversationModel(params.model, extra),\n      extra\n    });\n    emitConversationListChanged(context, conversation.id, \"created\");\n    return conversation;\n  });\n  router2.register(\"remove-conversation\", (data, context) => {\n    const params = asRecord(data);\n    const conversationId = stringParam(params.id);\n    const removed = store.removeConversation(conversationId);\n    if (removed) {\n      emitConversationListChanged(context, conversationId, \"deleted\");\n    }\n    return removed;\n  });\n  router2.register(\"update-conversation\", (data, context) => {\n    const params = asRecord(data);\n    const conversationId = stringParam(params.id);\n    const updated = store.updateConversation(conversationId, asRecord(params.updates));\n    if (updated) {\n      emitConversationListChanged(context, conversationId, \"updated\");\n    }\n    return updated;\n  });\n  router2.register(\"chat.send.message\", async (data, context) => {\n    const params = asRecord(data);\n    const conversationId = stringParam(params.conversation_id);\n    const input = stringParam(params.input);\n    const userMsgId = typeof params.msg_id === \"string\" ? params.msg_id : void 0;\n    const assistantMsgId = `assistant_${userMsgId || Date.now().toString(36)}`;\n    const conversation = requireConversation(store, conversationId);\n    const files = mergeFiles(conversation.extra.defaultFiles, params.files);\n    const backend = conversation.extra.backend || \"opencode\";\n    const adapter = adapters.get(backend);\n    if (!adapter) {\n      throw new Error(`Agent adapter '${backend}' was not found`);\n    }\n    await stopActiveRun(activeRuns, activeRunBackends, adapters, conversationId);\n    const runController = new AbortController();\n    const acceptedRuntime = runningRuntimeSummary(\n      \"running\",\n      assistantMsgId,\n      pendingConfirmationCount(pendingConfirmations, conversationId)\n    );\n    activeRuns.set(conversationId, runController);\n    activeRunBackends.set(conversationId, backend);\n    activeTurnIds.set(conversationId, assistantMsgId);\n    store.updateConversation(conversationId, {\n      status: \"running\",\n      runtime: acceptedRuntime\n    });\n    const userMessage = store.addTextMessage({\n      conversationId,\n      msgId: userMsgId,\n      position: \"right\",\n      content: input\n    });\n    context.emit(\"message.userCreated\", buildUserCreatedEvent(userMessage));\n    context.emit(\"chat.response.stream\", {\n      type: \"start\",\n      msg_id: assistantMsgId,\n      conversation_id: conversationId,\n      data: null\n    });\n    context.runDetached(async () => {\n      let assistantContent = \"\";\n      try {\n        for await (const event of adapter.sendMessage({\n          conversationId,\n          input,\n          msgId: userMessage.msg_id,\n          workspace: conversation.extra.workspace,\n          model: typeof conversation.extra.currentModelId === \"string\" ? conversation.extra.currentModelId : void 0,\n          sessionMode: typeof conversation.extra.sessionMode === \"string\" ? conversation.extra.sessionMode : void 0,\n          signal: runController.signal,\n          ...files.length ? { files } : {}\n        })) {\n          switch (event.type) {\n            case \"thought\":\n              emitChatStream(context, conversationId, assistantMsgId, \"thought\", {\n                subject: event.subject,\n                description: event.description\n              });\n              break;\n            case \"thinking\":\n              emitChatStream(context, conversationId, assistantMsgId, \"thinking\", {\n                content: event.content,\n                ...event.subject ? { subject: event.subject } : {},\n                ...event.duration !== void 0 ? { duration: event.duration } : {},\n                status: event.status ?? \"thinking\"\n              });\n              break;\n            case \"tool_call\":\n              store.upsertStructuredMessage({\n                conversationId,\n                msgId: assistantMsgId,\n                type: \"tool_call\",\n                content: event.data,\n                identityKeys: [\"callId\", \"toolCallId\"]\n              });\n              emitChatStream(context, conversationId, assistantMsgId, \"tool_call\", event.data);\n              break;\n            case \"tool_group\":\n              for (const tool of event.tools) {\n                store.upsertToolGroupMessage({ conversationId, msgId: assistantMsgId, tool });\n              }\n              emitChatStream(context, conversationId, assistantMsgId, \"tool_group\", event.tools);\n              break;\n            case \"acp_tool_call\":\n              store.upsertStructuredMessage({\n                conversationId,\n                msgId: assistantMsgId,\n                type: \"acp_tool_call\",\n                content: event.data,\n                identityKeys: [\"toolCallId\", \"callId\"]\n              });\n              emitChatStream(context, conversationId, assistantMsgId, \"acp_tool_call\", event.data);\n              break;\n            case \"codex_tool_call\":\n              store.upsertStructuredMessage({\n                conversationId,\n                msgId: assistantMsgId,\n                type: \"codex_tool_call\",\n                content: event.data,\n                identityKeys: [\"toolCallId\", \"callId\"]\n              });\n              emitChatStream(context, conversationId, assistantMsgId, \"codex_tool_call\", event.data);\n              break;\n            case \"plan\":\n              store.upsertStructuredMessage({\n                conversationId,\n                msgId: assistantMsgId,\n                type: \"plan\",\n                content: event.data,\n                identityKeys: [\"sessionId\"]\n              });\n              emitChatStream(context, conversationId, assistantMsgId, \"plan\", event.data);\n              break;\n            case \"context_usage\":\n              store.updateConversation(conversationId, {\n                extra: {\n                  lastContextUsage: {\n                    used: event.used,\n                    size: event.size\n                  }\n                }\n              });\n              emitChatStream(context, conversationId, assistantMsgId, \"acp_context_usage\", {\n                used: event.used,\n                size: event.size\n              });\n              break;\n            case \"artifact\": {\n              const artifact = store.upsertArtifact(normalizeAdapterArtifact(event.artifact, conversationId));\n              context.emit(\"conversation.artifact\", artifact);\n              break;\n            }\n            case \"agent_status\":\n              emitChatStream(context, conversationId, assistantMsgId, \"agent_status\", event.data);\n              break;\n            case \"available_commands\":\n              emitChatStream(context, conversationId, assistantMsgId, \"available_commands\", {\n                commands: event.commands\n              });\n              break;\n            case \"permission\": {\n              const confirmation = normalizeConfirmation(event.confirmation, conversationId, assistantMsgId);\n              pendingConfirmations.set(confirmation.id, { backend, confirmation });\n              store.updateConversation(conversationId, {\n                status: \"waiting_confirmation\",\n                runtime: runningRuntimeSummary(\n                  \"waiting_confirmation\",\n                  assistantMsgId,\n                  pendingConfirmationCount(pendingConfirmations, conversationId)\n                )\n              });\n              context.emit(\"confirmation.add\", confirmation);\n              break;\n            }\n            case \"permission_resolved\":\n              if (pendingConfirmations.delete(event.confirmationId)) {\n                if (activeRuns.get(conversationId) === runController) {\n                  store.updateConversation(conversationId, {\n                    status: \"running\",\n                    runtime: runningRuntimeSummary(\n                      \"running\",\n                      assistantMsgId,\n                      pendingConfirmationCount(pendingConfirmations, conversationId)\n                    )\n                  });\n                }\n                context.emit(\"confirmation.remove\", { conversation_id: conversationId, id: event.confirmationId });\n              }\n              break;\n            case \"content\":\n              assistantContent += event.content;\n              emitChatStream(context, conversationId, assistantMsgId, \"content\", { content: event.content });\n              break;\n          }\n        }\n        if (runController.signal.aborted && !assistantContent) {\n          assistantContent = \"Generation stopped.\";\n          emitChatStream(context, conversationId, assistantMsgId, \"content\", { content: assistantContent });\n        }\n      } catch (error) {\n        if (runController.signal.aborted || isAbortError(error)) {\n          if (!assistantContent) {\n            assistantContent = \"Generation stopped.\";\n            emitChatStream(context, conversationId, assistantMsgId, \"content\", { content: assistantContent });\n          }\n        } else {\n          const failureContent = formatRuntimeFailureContent(backend, error);\n          const contentChunk = assistantContent ? `\n\n${failureContent}` : failureContent;\n          assistantContent += contentChunk;\n          emitChatStream(context, conversationId, assistantMsgId, \"content\", { content: contentChunk });\n        }\n      } finally {\n        const activeRun = activeRuns.get(conversationId);\n        if (activeRun === runController) {\n          activeRuns.delete(conversationId);\n          activeRunBackends.delete(conversationId);\n          activeTurnIds.delete(conversationId);\n        }\n        if (activeRun === runController || activeRun === void 0) {\n          store.updateConversation(conversationId, {\n            status: \"finished\",\n            runtime: idleRuntimeSummary(\"finished\", pendingConfirmationCount(pendingConfirmations, conversationId))\n          });\n        }\n      }\n      const assistantMessage = store.addTextMessage({\n        conversationId,\n        msgId: assistantMsgId,\n        position: \"left\",\n        content: assistantContent\n      });\n      context.emit(\"chat.response.stream\", {\n        type: \"finish\",\n        msg_id: assistantMsgId,\n        conversation_id: conversationId,\n        data: null\n      });\n      const completedConversation = requireConversation(store, conversationId);\n      context.emit(\n        \"turn.completed\",\n        buildTurnCompletedEvent({\n          conversation: completedConversation,\n          conversationId,\n          turnId: assistantMsgId,\n          backend,\n          assistantMessage\n        })\n      );\n    });\n    return {\n      success: true,\n      msg_id: userMessage.msg_id,\n      turn_id: assistantMsgId,\n      runtime: acceptedRuntime\n    };\n  });\n  router2.register(\"chat.stop.stream\", async (data) => {\n    const conversationId = stringParam(asRecord(data).conversation_id);\n    const stopped = await stopActiveRun(activeRuns, activeRunBackends, adapters, conversationId);\n    activeTurnIds.delete(conversationId);\n    const runtime = idleRuntimeSummary(\"finished\", pendingConfirmationCount(pendingConfirmations, conversationId));\n    if (stopped) {\n      store.updateConversation(conversationId, {\n        status: \"finished\",\n        runtime\n      });\n    }\n    return {\n      success: true,\n      stopped,\n      runtime: stopped ? runtime : store.getConversation(conversationId)?.runtime ?? runtime\n    };\n  });\n  router2.register(\"confirmation.list\", (data) => {\n    const params = asRecord(data);\n    const conversationId = stringParam(params.conversation_id);\n    return [...pendingConfirmations.values()].map((record) => record.confirmation).filter((confirmation) => confirmation.conversation_id === conversationId);\n  });\n  router2.register(\"confirmation.confirm\", async (data, context) => {\n    const params = asRecord(data);\n    const confirmationId = stringParam(params.msg_id ?? params.id);\n    const record = pendingConfirmations.get(confirmationId);\n    if (!record) {\n      return {\n        success: false,\n        error: { code: \"CONFIRMATION_NOT_FOUND\", message: `Confirmation not found: ${confirmationId}` }\n      };\n    }\n    const adapter = adapters.get(record.backend);\n    const recordConversationId = stringParam(record.confirmation.conversation_id);\n    const callId = typeof params.callId === \"string\" ? params.callId : typeof params.call_id === \"string\" ? params.call_id : record.confirmation.callId ?? record.confirmation.call_id;\n    const result = await adapter?.confirm?.({\n      conversationId: recordConversationId,\n      confirmationId,\n      ...callId ? { callId } : {},\n      data: params.data\n    });\n    pendingConfirmations.delete(confirmationId);\n    if (activeRuns.has(recordConversationId)) {\n      store.updateConversation(recordConversationId, {\n        status: \"running\",\n        runtime: runningRuntimeSummary(\n          \"running\",\n          activeTurnIds.get(recordConversationId) ?? null,\n          pendingConfirmationCount(pendingConfirmations, recordConversationId)\n        )\n      });\n    }\n    context.emit(\"confirmation.remove\", {\n      conversation_id: record.confirmation.conversation_id,\n      id: confirmationId\n    });\n    return result ?? { success: true };\n  });\n  router2.register(\"conversation.get-workspace\", async (data) => await getWorkspaceTree(asRecord(data)));\n  router2.register(\"workspace.removeEntry\", async (data) => await removeWorkspaceEntry(asRecord(data)));\n  router2.register(\"workspace.renameEntry\", async (data) => await renameWorkspaceEntry(asRecord(data)));\n  router2.register(\"fileSnapshot.compare\", async (data) => await compareWorkspaceChanges(asRecord(data)));\n  router2.register(\"fileSnapshot.diff\", async (data) => await readWorkspaceFileDiff(asRecord(data)));\n  router2.register(\"fileSnapshot.stageFile\", async (data) => await stageWorkspaceFile(asRecord(data)));\n  router2.register(\"fileSnapshot.stageAll\", async (data) => await stageWorkspace(asRecord(data)));\n  router2.register(\"fileSnapshot.unstageFile\", async (data) => await unstageWorkspaceFile(asRecord(data)));\n  router2.register(\"fileSnapshot.unstageAll\", async (data) => await unstageWorkspace(asRecord(data)));\n  router2.register(\"fileSnapshot.discardFile\", async (data) => await discardWorkspaceFile(asRecord(data)));\n  router2.register(\"get-file-by-dir\", async (data) => await getFileTreeByDir(asRecord(data)));\n  router2.register(\"read-file\", async (data) => await readTextFile(stringParam(asRecord(data).path)));\n  router2.register(\"get-image-base64\", async (data) => await readImageBase64(stringParam(asRecord(data).path)));\n  return router2;\n}\nfunction runningRuntimeSummary(status, turnId, pendingConfirmations) {\n  return {\n    state: status,\n    can_send_message: false,\n    has_task: true,\n    task_status: status,\n    is_processing: true,\n    pending_confirmations: pendingConfirmations,\n    turn_id: turnId\n  };\n}\nfunction idleRuntimeSummary(status, pendingConfirmations) {\n  return {\n    state: \"idle\",\n    can_send_message: true,\n    has_task: false,\n    task_status: status,\n    is_processing: false,\n    pending_confirmations: pendingConfirmations,\n    turn_id: null\n  };\n}\nfunction pendingConfirmationCount(pendingConfirmations, conversationId) {\n  let count = 0;\n  for (const { confirmation } of pendingConfirmations.values()) {\n    if (confirmation.conversation_id === conversationId) count += 1;\n  }\n  return count;\n}\nasync function stopActiveRun(activeRuns, activeRunBackends, adapters, conversationId) {\n  const controller = activeRuns.get(conversationId);\n  if (!controller) return false;\n  const backend = activeRunBackends.get(conversationId);\n  controller.abort(new Error(\"Generation stopped by user\"));\n  activeRuns.delete(conversationId);\n  activeRunBackends.delete(conversationId);\n  if (backend) {\n    try {\n      await adapters.get(backend)?.abort?.({ conversationId });\n    } catch {\n    }\n  }\n  return true;\n}\nfunction isAbortError(error) {\n  return error instanceof Error && error.name === \"AbortError\" || error instanceof Error && error.message.toLowerCase().includes(\"aborted\");\n}\nfunction formatRuntimeFailureContent(backend, error) {\n  return `${runtimeDisplayName(backend)} runtime failed: ${runtimeErrorMessage(error)}`;\n}\nfunction runtimeDisplayName(backend) {\n  if (backend === \"opencode\") return \"OpenCode\";\n  if (backend === \"gemini\") return \"Gemini CLI\";\n  if (backend === \"codex\") return \"Codex CLI\";\n  return \"Agent\";\n}\nfunction runtimeErrorMessage(error) {\n  const rawMessage = error instanceof Error ? error.message : typeof error === \"string\" ? error : \"Unknown runtime error\";\n  const message = redactRuntimeErrorText(rawMessage).trim();\n  return message.length ? truncateRuntimeErrorText(message) : \"Unknown runtime error\";\n}\nfunction redactRuntimeErrorText(text) {\n  return text.replace(/\\b(authorization\\s*:\\s*bearer\\s+)[^\\s,;]+/gi, \"$1[redacted]\").replace(/\\b([A-Z0-9_]*(?:api[_-]?key|token|secret|password)[A-Z0-9_]*\\s*[=:]\\s*)[^\\s,;]+/gi, \"$1[redacted]\").replace(/\\b(sk-[A-Za-z0-9_-]{8,})\\b/g, \"[redacted]\");\n}\nfunction truncateRuntimeErrorText(text) {\n  const maxLength = 600;\n  if (text.length <= maxLength) return text;\n  return `${text.slice(0, maxLength)}...`;\n}\nfunction emitChatStream(context, conversationId, msgId, type, data) {\n  context.emit(\"chat.response.stream\", {\n    type,\n    msg_id: msgId,\n    conversation_id: conversationId,\n    data\n  });\n}\nfunction normalizeAdapterArtifact(artifact, conversationId) {\n  if (artifact.conversation_id === conversationId) return artifact;\n  return {\n    ...artifact,\n    conversation_id: conversationId\n  };\n}\nfunction buildTurnCompletedEvent({\n  conversation,\n  conversationId,\n  turnId,\n  backend,\n  assistantMessage\n}) {\n  const runtime = conversation.runtime ?? idleRuntimeSummary(\"finished\", 0);\n  return {\n    session_id: conversationId,\n    turn_id: turnId,\n    status: conversation.status ?? \"finished\",\n    state: turnStateFromRuntime(runtime),\n    detail: \"\",\n    can_send_message: runtime.can_send_message,\n    runtime,\n    workspace: conversation.extra.workspace ?? \"\",\n    model: {\n      platform: backend,\n      name: conversation.extra.agentName || runtimeDisplayName(backend),\n      use_model: conversation.model.useModel || conversation.extra.currentModelLabel || conversation.extra.currentModelId || \"\"\n    },\n    last_message: {\n      id: assistantMessage.id,\n      type: assistantMessage.type,\n      content: assistantMessage.content,\n      status: \"finish\",\n      created_at: assistantMessage.createdAt\n    }\n  };\n}\nfunction buildUserCreatedEvent(message) {\n  return {\n    conversation_id: message.conversation_id,\n    msg_id: message.msg_id,\n    content: message.content.content,\n    position: \"right\",\n    status: \"finish\",\n    hidden: false,\n    created_at: message.createdAt\n  };\n}\nfunction emitConversationListChanged(context, conversationId, action) {\n  context.emit(\"conversation.listChanged\", {\n    conversation_id: conversationId,\n    action,\n    source: \"local\"\n  });\n}\nfunction turnStateFromRuntime(runtime) {\n  if (runtime.state === \"idle\") return \"ai_waiting_input\";\n  if (runtime.state === \"waiting_confirmation\") return \"ai_waiting_confirmation\";\n  if (runtime.state === \"starting\") return \"initializing\";\n  if (runtime.state === \"cancelling\") return \"stopped\";\n  if (runtime.state === \"running\") return \"ai_generating\";\n  return \"unknown\";\n}\nfunction normalizeConfirmation(confirmation, conversationId, msgId) {\n  const callId = confirmation.callId ?? confirmation.call_id;\n  return {\n    ...confirmation,\n    msg_id: confirmation.msg_id ?? msgId,\n    conversation_id: confirmation.conversation_id ?? conversationId,\n    ...callId ? { callId, call_id: confirmation.call_id ?? callId } : {}\n  };\n}\nasync function getAdapterModelInfo(adapters, backend) {\n  const adapter = adapters.get(backend);\n  if (!adapter?.getModelInfo) return null;\n  try {\n    return await adapter.getModelInfo();\n  } catch {\n    return null;\n  }\n}\nfunction requireConversation(store, conversationId) {\n  const conversation = store.getConversation(conversationId);\n  if (!conversation) {\n    throw new Error(`Conversation '${conversationId}' was not found`);\n  }\n  return conversation;\n}\nasync function buildConversationConfigOptions(adapters, conversation) {\n  const backend = conversation.extra.backend || conversation.type;\n  const configOptions = [];\n  const modelInfo = await getAdapterModelInfo(adapters, backend);\n  if (modelInfo?.canSwitch && modelInfo.availableModels.length > 0) {\n    configOptions.push({\n      id: \"model\",\n      name: \"Model\",\n      label: \"Model\",\n      category: \"model\",\n      type: \"select\",\n      option_type: \"select\",\n      current_value: conversation.extra.currentModelId ?? modelInfo.currentModelId ?? null,\n      options: modelInfo.availableModels.map((model) => ({\n        value: model.id,\n        label: model.label\n      }))\n    });\n  }\n  const modes = getAgentModes(backend);\n  if (modes.length > 0) {\n    configOptions.push({\n      id: \"mode\",\n      name: \"Permission\",\n      label: \"Permission\",\n      category: \"mode\",\n      type: \"select\",\n      option_type: \"select\",\n      current_value: conversation.extra.sessionMode ?? modes[0]?.value ?? null,\n      options: modes.map((mode) => ({\n        value: mode.value,\n        label: mode.label,\n        ...mode.description ? { description: mode.description } : {}\n      }))\n    });\n  }\n  return configOptions;\n}\nfunction modelInfoFromConfigOptions(configOptions) {\n  const modelOption = configOptions.find((option) => option.category === \"model\" || option.id === \"model\");\n  if (!modelOption) return null;\n  const currentModelId = modelOption.current_value ?? null;\n  const currentModel = modelOption.options.find((option) => option.value === currentModelId);\n  return {\n    currentModelId,\n    currentModelLabel: currentModel?.label ?? currentModel?.name ?? currentModelId,\n    availableModels: modelOption.options.map((option) => ({\n      id: option.value,\n      label: option.label || option.name || option.value\n    })),\n    canSwitch: modelOption.options.length > 0,\n    source: \"configOption\",\n    configOptionId: modelOption.id\n  };\n}\nfunction getConfigOptionConversationUpdates(option, value, selected) {\n  const category = option.category || option.id;\n  if (category === \"model\" || option.id === \"model\") {\n    const label = selected?.label || selected?.name || value;\n    return {\n      model: {\n        id: value,\n        useModel: label\n      },\n      extra: {\n        currentModelId: value,\n        currentModelLabel: label\n      }\n    };\n  }\n  if (category === \"mode\" || option.id === \"mode\") {\n    return {\n      extra: {\n        sessionMode: value\n      }\n    };\n  }\n  return {};\n}\nasync function getRuntimeStatus(adapters = createDefaultAgentAdapterRegistry(), environment = process.env) {\n  const dataRoot2 = environment.AICLIUI_HOME ?? join2(homedir(), \".aicliui\");\n  const bootstrap = await readBootstrapStatus(\n    environment.AICLIUI_BOOTSTRAP_STATUS ?? join2(dataRoot2, \"daemon\", \"bootstrap.status\")\n  );\n  return {\n    daemon: {\n      version: \"0.1.0\",\n      startedAt,\n      pid: process.pid\n    },\n    ...bootstrap ? { bootstrap } : {},\n    termux: {\n      runCommandPermission: runtimePermissionValue(environment.AICLIUI_TERMUX_RUN_COMMAND_PERMISSION),\n      allowExternalApps: externalAppsValue(environment.AICLIUI_TERMUX_ALLOW_EXTERNAL_APPS)\n    },\n    agents: await adapters.probeAll()\n  };\n}\nasync function readBootstrapStatus(path) {\n  try {\n    const values = Object.fromEntries(\n      (await readFile2(path, \"utf8\")).split(/\\r?\\n/).filter(Boolean).map((line) => {\n        const separator = line.indexOf(\"=\");\n        return separator === -1 ? [line, \"\"] : [line.slice(0, separator), line.slice(separator + 1)];\n      })\n    );\n    const updatedAt = Number(values.updatedAt);\n    return {\n      phase: typeof values.phase === \"string\" && values.phase ? values.phase : \"unknown\",\n      ...typeof values.detail === \"string\" && values.detail ? { detail: values.detail } : {},\n      ...Number.isFinite(updatedAt) ? { updatedAt } : {}\n    };\n  } catch {\n    return void 0;\n  }\n}\nfunction runtimePermissionValue(value) {\n  if (value === \"granted\" || value === \"denied\") return value;\n  return \"unknown\";\n}\nfunction externalAppsValue(value) {\n  if (value === \"enabled\" || value === \"disabled\") return value;\n  return \"unknown\";\n}\nfunction asRecord(value) {\n  return isRecord8(value) ? value : {};\n}\nfunction isRecord8(value) {\n  return typeof value === \"object\" && value !== null;\n}\nfunction stringParam(value, fallback) {\n  if (typeof value === \"string\") return value;\n  if (fallback !== void 0) return fallback;\n  throw new Error(\"Expected string bridge parameter\");\n}\nfunction numberParam(value, fallback) {\n  return typeof value === \"number\" && Number.isFinite(value) ? value : fallback;\n}\nfunction artifactStatusParam(value) {\n  if (value === \"active\" || value === \"pending\" || value === \"dismissed\" || value === \"saved\") return value;\n  throw new Error(`Unsupported artifact status '${String(value)}'`);\n}\nfunction stringArrayParam(value) {\n  return Array.isArray(value) ? value.filter((item) => typeof item === \"string\") : [];\n}\nfunction mergeFiles(fallback, primary) {\n  const seen = /* @__PURE__ */ new Set();\n  const files = [];\n  for (const filePath of [...stringArrayParam(fallback), ...stringArrayParam(primary)]) {\n    if (seen.has(filePath)) continue;\n    seen.add(filePath);\n    files.push(filePath);\n  }\n  return files;\n}\nfunction isModel(value) {\n  return isRecord8(value) && typeof value.id === \"string\" && typeof value.useModel === \"string\";\n}\nfunction normalizeConversationModel(model, extra) {\n  if (isModel(model) && (model.id || model.useModel)) {\n    return model;\n  }\n  if (typeof extra.currentModelId !== \"string\" || extra.currentModelId.length === 0) {\n    return { id: \"\", useModel: \"\" };\n  }\n  return {\n    id: extra.currentModelId,\n    useModel: typeof extra.currentModelLabel === \"string\" && extra.currentModelLabel.length > 0 ? extra.currentModelLabel : extra.currentModelId\n  };\n}\n\n// packages/daemon/src/index.ts\nvar port = Number.parseInt(process.env.AICLIUI_DAEMON_PORT ?? \"43117\", 10);\nvar token = process.env.AICLIUI_DAEMON_TOKEN;\nvar dataRoot = process.env.AICLIUI_HOME ?? join3(homedir2(), \".aicliui\");\nvar storePath = process.env.AICLIUI_STORE_PATH ?? join3(dataRoot, \"daemon\", \"store.json\");\nvar router = createDefaultRouter({ store: new JsonConversationStore(storePath) });\nvar server = new WebSocketServer({\n  host: \"127.0.0.1\",\n  port\n});\nserver.on(\"connection\", (socket, request) => {\n  if (token && !isAuthorized(request.headers[\"sec-websocket-protocol\"], token)) {\n    socket.close(1008, \"auth_failed\");\n    return;\n  }\n  socket.on(\"message\", (raw) => {\n    void (async () => {\n      const incoming = parseMessage(raw.toString());\n      if (!incoming) {\n        return;\n      }\n      if (incoming.name === \"pong\") {\n        return;\n      }\n      const responses = await router.handleIncoming(incoming, {\n        emit(response) {\n          if (socket.readyState === socket.OPEN) {\n            socket.send(JSON.stringify(response));\n          }\n        }\n      });\n      for (const response of responses) {\n        if (socket.readyState === socket.OPEN) {\n          socket.send(JSON.stringify(response));\n        }\n      }\n    })();\n  });\n  const heartbeat = setInterval(() => {\n    if (socket.readyState === socket.OPEN) {\n      socket.send(JSON.stringify({ name: \"ping\", data: { timestamp: Date.now() } }));\n    }\n  }, 15e3);\n  socket.on(\"close\", () => clearInterval(heartbeat));\n});\nserver.on(\"listening\", () => {\n  const address = server.address();\n  const resolvedPort = typeof address === \"object\" && address ? address.port : port;\n  console.log(`aicliui daemon listening on ws://127.0.0.1:${resolvedPort}`);\n});\nfunction parseMessage(raw) {\n  try {\n    const value = JSON.parse(raw);\n    if (value && typeof value === \"object\" && typeof value.name === \"string\") {\n      return value;\n    }\n  } catch {\n    return null;\n  }\n  return null;\n}\nfunction isAuthorized(protocolHeader, expected) {\n  if (Array.isArray(protocolHeader)) {\n    return protocolHeader.includes(expected);\n  }\n  return protocolHeader === expected;\n}\n";
